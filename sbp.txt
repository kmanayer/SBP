(herald "Session Binding Protocol (SBP)")

(comment "CPSA 3.6.6")
(comment "All input read from sbp.lsp")

(defprotocol sbp basic
  (defrole client
    (vars (answer cookie tlskey data))
    (trace (recv cookie) (send (enc "request" cookie tlskey))
      (recv (enc "response" answer cookie tlskey))))
  (defrole proxy
    (vars (answer cookie tlskey data))
    (trace (send cookie) (recv (enc "request" cookie tlskey))
      (send (enc "response" answer cookie tlskey)))
    (uniq-gen answer)))

(defskeleton sbp
  (vars (answer tlskey cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (deflistener answer)
  (pen-non-orig tlskey)
  (traces
    ((recv cookie) (send (enc "request" cookie tlskey))
      (recv (enc "response" answer cookie tlskey)))
    ((recv answer) (send answer)))
  (label 0)
  (unrealized (0 2))
  (origs)
  (comment "2 in cohort - 2 not yet seen"))

(defskeleton sbp
  (vars (answer tlskey cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (precedes ((2 2) (0 2)) ((2 2) (1 0)))
  (pen-non-orig tlskey)
  (uniq-gen answer)
  (operation encryption-test (added-strand proxy 3)
    (enc "response" answer cookie tlskey) (0 2))
  (traces
    ((recv cookie) (send (enc "request" cookie tlskey))
      (recv (enc "response" answer cookie tlskey)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie tlskey))
      (send (enc "response" answer cookie tlskey))))
  (label 1)
  (parent 0)
  (unrealized (1 0) (2 1))
  (comment "3 in cohort - 3 not yet seen"))

(defskeleton sbp
  (vars (answer tlskey cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (deflistener answer)
  (deflistener tlskey)
  (precedes ((2 1) (0 2)))
  (pen-non-orig tlskey)
  (operation encryption-test (added-listener tlskey)
    (enc "response" answer cookie tlskey) (0 2))
  (traces
    ((recv cookie) (send (enc "request" cookie tlskey))
      (recv (enc "response" answer cookie tlskey)))
    ((recv answer) (send answer)) ((recv tlskey) (send tlskey)))
  (label 2)
  (parent 0)
  (unrealized (2 0))
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer tlskey cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (precedes ((0 1) (2 1)) ((2 2) (0 2)) ((2 2) (1 0)))
  (pen-non-orig tlskey)
  (uniq-gen answer)
  (operation encryption-test (displaced 3 0 client 2)
    (enc "request" cookie tlskey) (2 1))
  (traces
    ((recv cookie) (send (enc "request" cookie tlskey))
      (recv (enc "response" answer cookie tlskey)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie tlskey))
      (send (enc "response" answer cookie tlskey))))
  (label 3)
  (parent 1)
  (unrealized (1 0))
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer tlskey cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (defstrand client 2 (cookie cookie) (tlskey tlskey))
  (precedes ((2 2) (0 2)) ((2 2) (1 0)) ((3 1) (2 1)))
  (pen-non-orig tlskey)
  (uniq-gen answer)
  (operation encryption-test (added-strand client 2)
    (enc "request" cookie tlskey) (2 1))
  (traces
    ((recv cookie) (send (enc "request" cookie tlskey))
      (recv (enc "response" answer cookie tlskey)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie tlskey))
      (send (enc "response" answer cookie tlskey)))
    ((recv cookie) (send (enc "request" cookie tlskey))))
  (label 4)
  (parent 1)
  (unrealized (1 0))
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer tlskey cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (deflistener tlskey)
  (precedes ((2 2) (0 2)) ((2 2) (1 0)) ((3 1) (2 1)))
  (pen-non-orig tlskey)
  (uniq-gen answer)
  (operation encryption-test (added-listener tlskey)
    (enc "request" cookie tlskey) (2 1))
  (traces
    ((recv cookie) (send (enc "request" cookie tlskey))
      (recv (enc "response" answer cookie tlskey)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie tlskey))
      (send (enc "response" answer cookie tlskey)))
    ((recv tlskey) (send tlskey)))
  (label 5)
  (parent 1)
  (unrealized (3 0))
  (comment "2 in cohort - 2 not yet seen"))

(defskeleton sbp
  (vars (answer tlskey cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (deflistener answer)
  (deflistener tlskey)
  (defstrand proxy 1 (cookie tlskey))
  (precedes ((2 1) (0 2)) ((3 0) (2 0)))
  (pen-non-orig tlskey)
  (operation nonce-test (added-strand proxy 1) tlskey (2 0))
  (traces
    ((recv cookie) (send (enc "request" cookie tlskey))
      (recv (enc "response" answer cookie tlskey)))
    ((recv answer) (send answer)) ((recv tlskey) (send tlskey))
    ((send tlskey)))
  (label 6)
  (parent 2)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer tlskey cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (deflistener tlskey)
  (precedes ((0 1) (2 1)) ((2 2) (0 2)) ((2 2) (1 0)) ((3 1) (1 0)))
  (pen-non-orig tlskey)
  (uniq-gen answer)
  (operation nonce-test (added-listener tlskey) answer (1 0)
    (enc "response" answer cookie tlskey))
  (traces
    ((recv cookie) (send (enc "request" cookie tlskey))
      (recv (enc "response" answer cookie tlskey)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie tlskey))
      (send (enc "response" answer cookie tlskey)))
    ((recv tlskey) (send tlskey)))
  (label 7)
  (parent 3)
  (unrealized (3 0))
  (comment "2 in cohort - 2 not yet seen"))

(defskeleton sbp
  (vars (answer tlskey cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (defstrand client 2 (cookie cookie) (tlskey tlskey))
  (deflistener tlskey)
  (precedes ((2 2) (0 2)) ((2 2) (1 0)) ((3 1) (2 1)) ((4 1) (1 0)))
  (pen-non-orig tlskey)
  (uniq-gen answer)
  (operation nonce-test (added-listener tlskey) answer (1 0)
    (enc "response" answer cookie tlskey))
  (traces
    ((recv cookie) (send (enc "request" cookie tlskey))
      (recv (enc "response" answer cookie tlskey)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie tlskey))
      (send (enc "response" answer cookie tlskey)))
    ((recv cookie) (send (enc "request" cookie tlskey)))
    ((recv tlskey) (send tlskey)))
  (label 8)
  (parent 4)
  (unrealized (4 0))
  (comment "2 in cohort - 2 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener cookie)
  (precedes ((2 0) (3 0)) ((2 2) (0 2)) ((2 2) (1 0)) ((3 1) (2 1)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation nonce-test (displaced 4 2 proxy 1) tlskey (3 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie)))
    ((recv cookie) (send cookie)))
  (label 9)
  (parent 5)
  (unrealized (0 0))
  (comment "2 in cohort - 2 not yet seen"))

(defskeleton sbp
  (vars (answer tlskey cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (deflistener tlskey)
  (defstrand proxy 1 (cookie tlskey))
  (precedes ((2 2) (0 2)) ((2 2) (1 0)) ((3 1) (2 1)) ((4 0) (3 0)))
  (pen-non-orig tlskey)
  (uniq-gen answer)
  (operation nonce-test (added-strand proxy 1) tlskey (3 0))
  (traces
    ((recv cookie) (send (enc "request" cookie tlskey))
      (recv (enc "response" answer cookie tlskey)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie tlskey))
      (send (enc "response" answer cookie tlskey)))
    ((recv tlskey) (send tlskey)) ((send tlskey)))
  (label 10)
  (parent 5)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer tlskey cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (deflistener answer)
  (defstrand proxy 1 (cookie tlskey))
  (precedes ((2 0) (0 2)))
  (pen-non-orig tlskey)
  (operation generalization deleted (2 0))
  (traces
    ((recv cookie) (send (enc "request" cookie tlskey))
      (recv (enc "response" answer cookie tlskey)))
    ((recv answer) (send answer)) ((send tlskey)))
  (label 11)
  (parent 6)
  (unrealized)
  (shape)
  (maps ((0 1) ((answer answer) (tlskey tlskey) (cookie cookie))))
  (origs))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener cookie)
  (precedes ((0 1) (2 1)) ((2 0) (3 0)) ((2 2) (0 2)) ((2 2) (1 0))
    ((3 1) (1 0)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation nonce-test (displaced 4 2 proxy 1) tlskey (3 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie)))
    ((recv cookie) (send cookie)))
  (label 12)
  (parent 7)
  (unrealized (0 0))
  (comment "2 in cohort - 2 not yet seen"))

(defskeleton sbp
  (vars (answer tlskey cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (deflistener tlskey)
  (defstrand proxy 1 (cookie tlskey))
  (precedes ((0 1) (2 1)) ((2 2) (0 2)) ((2 2) (1 0)) ((3 1) (1 0))
    ((4 0) (3 0)))
  (pen-non-orig tlskey)
  (uniq-gen answer)
  (operation nonce-test (added-strand proxy 1) tlskey (3 0))
  (traces
    ((recv cookie) (send (enc "request" cookie tlskey))
      (recv (enc "response" answer cookie tlskey)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie tlskey))
      (send (enc "response" answer cookie tlskey)))
    ((recv tlskey) (send tlskey)) ((send tlskey)))
  (label 13)
  (parent 7)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (defstrand client 2 (cookie cookie) (tlskey cookie))
  (deflistener cookie)
  (precedes ((2 0) (4 0)) ((2 2) (0 2)) ((2 2) (1 0)) ((3 1) (2 1))
    ((4 1) (1 0)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation nonce-test (displaced 5 2 proxy 1) tlskey (4 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie)))
    ((recv cookie) (send (enc "request" cookie cookie)))
    ((recv cookie) (send cookie)))
  (label 14)
  (parent 8)
  (unrealized (0 0) (3 0))
  (comment "2 in cohort - 2 not yet seen"))

(defskeleton sbp
  (vars (answer tlskey cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (defstrand client 2 (cookie cookie) (tlskey tlskey))
  (deflistener tlskey)
  (defstrand proxy 1 (cookie tlskey))
  (precedes ((2 2) (0 2)) ((2 2) (1 0)) ((3 1) (2 1)) ((4 1) (1 0))
    ((5 0) (4 0)))
  (pen-non-orig tlskey)
  (uniq-gen answer)
  (operation nonce-test (added-strand proxy 1) tlskey (4 0))
  (traces
    ((recv cookie) (send (enc "request" cookie tlskey))
      (recv (enc "response" answer cookie tlskey)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie tlskey))
      (send (enc "response" answer cookie tlskey)))
    ((recv cookie) (send (enc "request" cookie tlskey)))
    ((recv tlskey) (send tlskey)) ((send tlskey)))
  (label 15)
  (parent 8)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener cookie)
  (precedes ((2 0) (0 0)) ((2 0) (3 0)) ((2 2) (0 2)) ((2 2) (1 0))
    ((3 1) (2 1)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation nonce-test (displaced 4 2 proxy 1) cookie (0 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie)))
    ((recv cookie) (send cookie)))
  (label 16)
  (parent 9)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener cookie)
  (defstrand proxy 1 (cookie cookie))
  (precedes ((2 0) (3 0)) ((2 2) (0 2)) ((2 2) (1 0)) ((3 1) (2 1))
    ((4 0) (0 0)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation nonce-test (added-strand proxy 1) cookie (0 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie)))
    ((recv cookie) (send cookie)) ((send cookie)))
  (label 17)
  (parent 9)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer tlskey cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (deflistener answer)
  (deflistener tlskey)
  (defstrand proxy 1 (cookie tlskey))
  (precedes ((2 1) (0 2)) ((2 1) (1 0)) ((3 0) (2 0)))
  (pen-non-orig tlskey)
  (operation generalization deleted (2 0))
  (traces
    ((recv cookie) (send (enc "request" cookie tlskey))
      (recv (enc "response" answer cookie tlskey)))
    ((recv answer) (send answer)) ((recv tlskey) (send tlskey))
    ((send tlskey)))
  (label 18)
  (parent 10)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener cookie)
  (precedes ((0 1) (2 1)) ((2 0) (0 0)) ((2 0) (3 0)) ((2 2) (0 2))
    ((2 2) (1 0)) ((3 1) (1 0)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation nonce-test (displaced 4 2 proxy 1) cookie (0 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie)))
    ((recv cookie) (send cookie)))
  (label 19)
  (parent 12)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener cookie)
  (defstrand proxy 1 (cookie cookie))
  (precedes ((0 1) (2 1)) ((2 0) (3 0)) ((2 2) (0 2)) ((2 2) (1 0))
    ((3 1) (1 0)) ((4 0) (0 0)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation nonce-test (added-strand proxy 1) cookie (0 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie)))
    ((recv cookie) (send cookie)) ((send cookie)))
  (label 20)
  (parent 12)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer tlskey cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (defstrand proxy 1 (cookie tlskey))
  (precedes ((0 1) (2 1)) ((2 2) (0 2)) ((2 2) (1 0)) ((3 0) (1 0)))
  (pen-non-orig tlskey)
  (uniq-gen answer)
  (operation generalization deleted (3 0))
  (traces
    ((recv cookie) (send (enc "request" cookie tlskey))
      (recv (enc "response" answer cookie tlskey)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie tlskey))
      (send (enc "response" answer cookie tlskey))) ((send tlskey)))
  (label 21)
  (parent 13)
  (unrealized)
  (shape)
  (maps ((0 1) ((answer answer) (tlskey tlskey) (cookie cookie))))
  (origs))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (defstrand client 2 (cookie cookie) (tlskey cookie))
  (deflistener cookie)
  (precedes ((2 0) (3 0)) ((2 0) (4 0)) ((2 2) (0 2)) ((2 2) (1 0))
    ((3 1) (2 1)) ((4 1) (1 0)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation nonce-test (displaced 5 2 proxy 1) cookie (3 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie)))
    ((recv cookie) (send (enc "request" cookie cookie)))
    ((recv cookie) (send cookie)))
  (label 22)
  (parent 14)
  (unrealized (0 0))
  (comment "2 in cohort - 2 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (defstrand client 2 (cookie cookie) (tlskey cookie))
  (deflistener cookie)
  (defstrand proxy 1 (cookie cookie))
  (precedes ((2 0) (4 0)) ((2 2) (0 2)) ((2 2) (1 0)) ((3 1) (2 1))
    ((4 1) (1 0)) ((5 0) (3 0)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation nonce-test (added-strand proxy 1) cookie (3 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie)))
    ((recv cookie) (send (enc "request" cookie cookie)))
    ((recv cookie) (send cookie)) ((send cookie)))
  (label 23)
  (parent 14)
  (unrealized (0 0))
  (comment "3 in cohort - 3 not yet seen"))

(defskeleton sbp
  (vars (answer tlskey cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (defstrand client 2 (cookie cookie) (tlskey tlskey))
  (defstrand proxy 1 (cookie tlskey))
  (precedes ((2 2) (0 2)) ((2 2) (1 0)) ((3 1) (2 1)) ((4 0) (1 0)))
  (pen-non-orig tlskey)
  (uniq-gen answer)
  (operation generalization deleted (4 0))
  (traces
    ((recv cookie) (send (enc "request" cookie tlskey))
      (recv (enc "response" answer cookie tlskey)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie tlskey))
      (send (enc "response" answer cookie tlskey)))
    ((recv cookie) (send (enc "request" cookie tlskey)))
    ((send tlskey)))
  (label 24)
  (parent 15)
  (unrealized)
  (shape)
  (maps ((0 1) ((answer answer) (tlskey tlskey) (cookie cookie))))
  (origs))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (precedes ((2 0) (0 0)) ((2 2) (0 2)) ((2 2) (1 0)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation generalization deleted (3 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie))))
  (label 25)
  (parent 16)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (defstrand proxy 1 (cookie cookie))
  (precedes ((2 2) (0 2)) ((2 2) (1 0)) ((3 0) (0 0)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation generalization deleted (3 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie))) ((send cookie)))
  (label 26)
  (parent 17)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer tlskey cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey tlskey))
  (deflistener answer)
  (defstrand proxy 1 (cookie tlskey))
  (precedes ((2 0) (0 2)) ((2 0) (1 0)))
  (pen-non-orig tlskey)
  (operation generalization deleted (2 0))
  (traces
    ((recv cookie) (send (enc "request" cookie tlskey))
      (recv (enc "response" answer cookie tlskey)))
    ((recv answer) (send answer)) ((send tlskey)))
  (label 27)
  (parent 18)
  (seen 11)
  (unrealized)
  (comment "1 in cohort - 0 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (precedes ((0 1) (2 1)) ((2 0) (0 0)) ((2 2) (0 2)) ((2 2) (1 0)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation generalization deleted (3 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie))))
  (label 28)
  (parent 19)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (defstrand proxy 1 (cookie cookie))
  (precedes ((0 1) (2 1)) ((2 2) (0 2)) ((2 2) (1 0)) ((3 0) (0 0)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation generalization deleted (3 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie))) ((send cookie)))
  (label 29)
  (parent 20)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (defstrand client 2 (cookie cookie) (tlskey cookie))
  (deflistener cookie)
  (precedes ((2 0) (0 0)) ((2 0) (3 0)) ((2 0) (4 0)) ((2 2) (0 2))
    ((2 2) (1 0)) ((3 1) (2 1)) ((4 1) (1 0)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation nonce-test (displaced 5 2 proxy 1) cookie (0 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie)))
    ((recv cookie) (send (enc "request" cookie cookie)))
    ((recv cookie) (send cookie)))
  (label 30)
  (parent 22)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (defstrand client 2 (cookie cookie) (tlskey cookie))
  (deflistener cookie)
  (defstrand proxy 1 (cookie cookie))
  (precedes ((2 0) (3 0)) ((2 0) (4 0)) ((2 2) (0 2)) ((2 2) (1 0))
    ((3 1) (2 1)) ((4 1) (1 0)) ((5 0) (0 0)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation nonce-test (added-strand proxy 1) cookie (0 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie)))
    ((recv cookie) (send (enc "request" cookie cookie)))
    ((recv cookie) (send cookie)) ((send cookie)))
  (label 31)
  (parent 22)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (defstrand client 2 (cookie cookie) (tlskey cookie))
  (deflistener cookie)
  (defstrand proxy 1 (cookie cookie))
  (precedes ((2 0) (0 0)) ((2 0) (4 0)) ((2 2) (0 2)) ((2 2) (1 0))
    ((3 1) (2 1)) ((4 1) (1 0)) ((5 0) (3 0)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation nonce-test (displaced 6 2 proxy 1) cookie (0 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie)))
    ((recv cookie) (send (enc "request" cookie cookie)))
    ((recv cookie) (send cookie)) ((send cookie)))
  (label 32)
  (parent 23)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (defstrand client 2 (cookie cookie) (tlskey cookie))
  (deflistener cookie)
  (defstrand proxy 1 (cookie cookie))
  (precedes ((2 0) (4 0)) ((2 2) (0 2)) ((2 2) (1 0)) ((3 1) (2 1))
    ((4 1) (1 0)) ((5 0) (0 0)) ((5 0) (3 0)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation nonce-test (displaced 6 5 proxy 1) cookie (0 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie)))
    ((recv cookie) (send (enc "request" cookie cookie)))
    ((recv cookie) (send cookie)) ((send cookie)))
  (label 33)
  (parent 23)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (defstrand client 2 (cookie cookie) (tlskey cookie))
  (deflistener cookie)
  (defstrand proxy 1 (cookie cookie))
  (defstrand proxy 1 (cookie cookie))
  (precedes ((2 0) (4 0)) ((2 2) (0 2)) ((2 2) (1 0)) ((3 1) (2 1))
    ((4 1) (1 0)) ((5 0) (3 0)) ((6 0) (0 0)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation nonce-test (added-strand proxy 1) cookie (0 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie)))
    ((recv cookie) (send (enc "request" cookie cookie)))
    ((recv cookie) (send cookie)) ((send cookie)) ((send cookie)))
  (label 34)
  (parent 23)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer cookie cookie-0 data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie-0))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie-0) (tlskey cookie))
  (precedes ((2 0) (0 0)) ((2 2) (0 2)) ((2 2) (1 0)))
  (pen-non-orig cookie-0)
  (uniq-gen answer)
  (operation generalization separated cookie-0)
  (traces
    ((recv cookie) (send (enc "request" cookie cookie-0))
      (recv (enc "response" answer cookie cookie-0)))
    ((recv answer) (send answer))
    ((send cookie-0) (recv (enc "request" cookie-0 cookie))
      (send (enc "response" answer cookie-0 cookie))))
  (label 35)
  (parent 25)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 1 (cookie cookie))
  (precedes ((2 0) (0 0)))
  (pen-non-orig cookie)
  (operation generalization deleted (2 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer)) ((send cookie)))
  (label 36)
  (parent 26)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (precedes ((0 1) (1 0)) ((2 0) (0 0)) ((2 2) (0 2)) ((2 2) (1 0)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation generalization weakened ((0 1) (2 1)))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie))))
  (label 37)
  (parent 28)
  (seen 25)
  (unrealized)
  (comment "1 in cohort - 0 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 1 (cookie cookie))
  (precedes ((0 1) (1 0)) ((2 0) (0 0)))
  (pen-non-orig cookie)
  (operation generalization deleted (2 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer)) ((send cookie)))
  (label 38)
  (parent 29)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener cookie)
  (precedes ((2 0) (0 0)) ((2 0) (3 0)) ((2 2) (0 2)) ((2 2) (1 0))
    ((3 1) (1 0)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation generalization deleted (3 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie)))
    ((recv cookie) (send cookie)))
  (label 39)
  (parent 30)
  (seen 25)
  (unrealized)
  (comment "1 in cohort - 0 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener cookie)
  (defstrand proxy 1 (cookie cookie))
  (precedes ((2 0) (3 0)) ((2 2) (0 2)) ((2 2) (1 0)) ((3 1) (1 0))
    ((4 0) (0 0)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation generalization deleted (3 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie)))
    ((recv cookie) (send cookie)) ((send cookie)))
  (label 40)
  (parent 31)
  (seen 26)
  (unrealized)
  (comment "1 in cohort - 0 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener cookie)
  (defstrand proxy 1 (cookie cookie))
  (precedes ((2 0) (0 0)) ((2 0) (3 0)) ((2 2) (0 2)) ((2 2) (1 0))
    ((3 1) (1 0)) ((4 0) (2 1)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation generalization deleted (3 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie)))
    ((recv cookie) (send cookie)) ((send cookie)))
  (label 41)
  (parent 32)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener cookie)
  (defstrand proxy 1 (cookie cookie))
  (precedes ((2 0) (3 0)) ((2 2) (0 2)) ((2 2) (1 0)) ((3 1) (1 0))
    ((4 0) (0 0)) ((4 0) (2 1)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation generalization deleted (3 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie)))
    ((recv cookie) (send cookie)) ((send cookie)))
  (label 42)
  (parent 33)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener cookie)
  (defstrand proxy 1 (cookie cookie))
  (defstrand proxy 1 (cookie cookie))
  (precedes ((2 0) (3 0)) ((2 2) (0 2)) ((2 2) (1 0)) ((3 1) (1 0))
    ((4 0) (2 1)) ((5 0) (0 0)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation generalization deleted (3 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie)))
    ((recv cookie) (send cookie)) ((send cookie)) ((send cookie)))
  (label 43)
  (parent 34)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer cookie cookie-0 data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie-0))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie-0) (tlskey cookie))
  (precedes ((2 2) (0 2)) ((2 2) (1 0)))
  (pen-non-orig cookie-0)
  (uniq-gen answer)
  (operation generalization weakened ((2 0) (0 0)))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie-0))
      (recv (enc "response" answer cookie cookie-0)))
    ((recv answer) (send answer))
    ((send cookie-0) (recv (enc "request" cookie-0 cookie))
      (send (enc "response" answer cookie-0 cookie))))
  (label 44)
  (parent 35)
  (unrealized)
  (shape)
  (maps ((0 1) ((answer answer) (tlskey cookie-0) (cookie cookie))))
  (origs))

(defskeleton sbp
  (vars (answer cookie cookie-0 data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie-0))
  (deflistener answer)
  (defstrand proxy 1 (cookie cookie-0))
  (precedes ((2 0) (0 0)))
  (pen-non-orig cookie-0)
  (operation generalization separated cookie-0)
  (traces
    ((recv cookie) (send (enc "request" cookie cookie-0))
      (recv (enc "response" answer cookie cookie-0)))
    ((recv answer) (send answer)) ((send cookie-0)))
  (label 45)
  (parent 36)
  (seen 11)
  (unrealized)
  (comment "1 in cohort - 0 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 1 (cookie cookie))
  (precedes ((2 0) (0 0)) ((2 0) (1 0)))
  (pen-non-orig cookie)
  (operation generalization weakened ((0 1) (1 0)))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer)) ((send cookie)))
  (label 46)
  (parent 38)
  (seen 36)
  (unrealized)
  (comment "1 in cohort - 0 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (defstrand proxy 1 (cookie cookie))
  (precedes ((2 0) (0 0)) ((2 2) (0 2)) ((2 2) (1 0)) ((3 0) (2 1)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation generalization deleted (3 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie))) ((send cookie)))
  (label 47)
  (parent 41)
  (seen 25)
  (unrealized)
  (comment "1 in cohort - 0 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (defstrand proxy 1 (cookie cookie))
  (precedes ((2 2) (0 2)) ((2 2) (1 0)) ((3 0) (0 0)) ((3 0) (2 1)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation generalization deleted (3 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie))) ((send cookie)))
  (label 48)
  (parent 42)
  (seen 46)
  (unrealized)
  (comment "1 in cohort - 0 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 3 (answer answer) (cookie cookie) (tlskey cookie))
  (defstrand proxy 1 (cookie cookie))
  (defstrand proxy 1 (cookie cookie))
  (precedes ((2 2) (0 2)) ((2 2) (1 0)) ((3 0) (2 1)) ((4 0) (0 0)))
  (pen-non-orig cookie)
  (uniq-gen answer)
  (operation generalization deleted (3 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer))
    ((send cookie) (recv (enc "request" cookie cookie))
      (send (enc "response" answer cookie cookie))) ((send cookie))
    ((send cookie)))
  (label 49)
  (parent 43)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (answer cookie data))
  (defstrand client 3 (answer answer) (cookie cookie) (tlskey cookie))
  (deflistener answer)
  (defstrand proxy 1 (cookie cookie))
  (defstrand proxy 1 (cookie cookie))
  (precedes ((2 0) (0 2)) ((2 0) (1 0)) ((3 0) (0 0)))
  (pen-non-orig cookie)
  (operation generalization deleted (2 0))
  (traces
    ((recv cookie) (send (enc "request" cookie cookie))
      (recv (enc "response" answer cookie cookie)))
    ((recv answer) (send answer)) ((send cookie)) ((send cookie)))
  (label 50)
  (parent 49)
  (seen 36)
  (unrealized)
  (comment "1 in cohort - 0 not yet seen"))

(comment "Nothing left to do")
