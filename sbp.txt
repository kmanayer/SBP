(herald "Session Binding Protocol (SBP)")

(comment "CPSA 3.6.6")
(comment "All input read from sbp.lsp")

(defprotocol sbp basic
  (defrole client
    (vars (challenge id secret request answer cookie syskey data)
      (proxy name))
    (trace (send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id)))))))
  (defrole proxy
    (vars (challenge id secret request answer cookie syskey data)
      (proxy name))
    (trace (recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request)
      (init request) (obsv (cat answer cookie))
      (send
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id)))))))
  (defrole server
    (vars (request answer cookie data))
    (trace (tran request (cat answer cookie)))))

(defskeleton sbp
  (vars (challenge secret id syskey request answer cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id)))))))
  (label 0)
  (unrealized (0 4) (0 6))
  (origs (challenge (0 0)) (secret (0 2)))
  (comment "3 in cohort - 3 not yet seen"))

(defskeleton sbp
  (vars (secret id syskey request answer cookie data) (proxy name))
  (defstrand client 7 (challenge id) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig secret id)
  (operation encryption-test (displaced 1 0 client 4)
    (enc id (hash secret id id)) (0 4))
  (traces
    ((send id) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret id id)))
      (recv (enc id (hash secret id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash secret id id)))))))
  (label 1)
  (parent 0)
  (unrealized (0 6))
  (origs (id (0 0)) (secret (0 2)))
  (comment "2 in cohort - 2 not yet seen"))

(defskeleton sbp
  (vars (challenge secret id syskey request answer cookie data)
    (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret secret)
    (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((1 4) (0 4)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation encryption-test (added-strand proxy 5)
    (enc challenge (hash secret challenge id)) (0 4))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy-0)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id)))))
  (label 2)
  (parent 0)
  (unrealized (0 6) (1 2) (1 3))
  (comment "3 in cohort - 3 not yet seen"))

(defskeleton sbp
  (vars (challenge secret id syskey request answer cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (deflistener (hash secret challenge id))
  (precedes ((1 1) (0 4)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation encryption-test (added-listener (hash secret challenge id))
    (enc challenge (hash secret challenge id)) (0 4))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv (hash secret challenge id))
      (send (hash secret challenge id))))
  (label 3)
  (parent 0)
  (unrealized (0 6) (1 0))
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (secret id syskey request answer cookie request-0 answer-0 data)
    (proxy proxy-0 name))
  (defstrand client 7 (challenge id) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((1 8) (0 6)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig secret id)
  (operation encryption-test (added-strand proxy 9)
    (enc cookie (hash syskey (hash secret id id))) (0 6))
  (traces
    ((send id) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret id id)))
      (recv (enc id (hash secret id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash secret id id))))))
    ((recv id) (send id) (recv (enc secret (pubk proxy-0)))
      (recv (enc id (hash secret id id)))
      (send (enc id (hash secret id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash secret id id)))))))
  (label 4)
  (parent 1)
  (unrealized (1 2) (1 3) (1 7))
  (comment "4 in cohort - 4 not yet seen"))

(defskeleton sbp
  (vars (secret id syskey request answer cookie data) (proxy name))
  (defstrand client 7 (challenge id) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (deflistener (hash syskey (hash secret id id)))
  (precedes ((1 1) (0 6)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig secret id)
  (operation encryption-test
    (added-listener (hash syskey (hash secret id id)))
    (enc cookie (hash syskey (hash secret id id))) (0 6))
  (traces
    ((send id) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret id id)))
      (recv (enc id (hash secret id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash secret id id))))))
    ((recv (hash syskey (hash secret id id)))
      (send (hash syskey (hash secret id id)))))
  (label 5)
  (parent 1)
  (unrealized (1 0))
  (dead)
  (comment "empty cohort"))

(defskeleton sbp
  (vars (challenge secret id syskey request answer cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret secret)
    (proxy proxy))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((1 4) (0 4)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation nonce-test (contracted (proxy-0 proxy)) secret (1 2)
    (enc secret (pubk proxy)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id)))))
  (label 6)
  (parent 2)
  (unrealized (0 6) (1 3))
  (comment "2 in cohort - 2 not yet seen"))

(defskeleton sbp
  (vars
    (challenge secret id syskey request answer cookie challenge-0 id-0
      secret-0 request-0 answer-0 syskey-0 data)
    (proxy proxy-0 proxy-1 name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret secret)
    (proxy proxy-0))
  (defstrand proxy 9 (challenge challenge-0) (id id-0) (secret secret-0)
    (request request-0) (answer answer-0) (cookie secret)
    (syskey syskey-0) (proxy proxy-1))
  (precedes ((0 0) (1 0)) ((0 2) (2 7)) ((1 4) (0 4)) ((2 8) (1 2)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation nonce-test (added-strand proxy 9) secret (1 2)
    (enc secret (pubk proxy)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy-0)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))))
    ((recv challenge-0) (send id-0) (recv (enc secret-0 (pubk proxy-1)))
      (recv (enc id-0 (hash secret-0 challenge-0 id-0)))
      (send (enc challenge-0 (hash secret-0 challenge-0 id-0)))
      (recv request-0) (init request-0) (obsv (cat answer-0 secret))
      (send
        (cat answer-0
          (enc secret
            (hash syskey-0 (hash secret-0 challenge-0 id-0)))))))
  (label 7)
  (parent 2)
  (unrealized (0 6) (2 7))
  (dead)
  (comment "empty cohort"))

(defskeleton sbp
  (vars
    (challenge secret id syskey request answer cookie challenge-0 id-0
      secret-0 request-0 cookie-0 syskey-0 data)
    (proxy proxy-0 proxy-1 name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret secret)
    (proxy proxy-0))
  (defstrand proxy 9 (challenge challenge-0) (id id-0) (secret secret-0)
    (request request-0) (answer secret) (cookie cookie-0)
    (syskey syskey-0) (proxy proxy-1))
  (precedes ((0 0) (1 0)) ((0 2) (2 7)) ((1 4) (0 4)) ((2 8) (1 2)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation nonce-test (added-strand proxy 9) secret (1 2)
    (enc secret (pubk proxy)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy-0)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))))
    ((recv challenge-0) (send id-0) (recv (enc secret-0 (pubk proxy-1)))
      (recv (enc id-0 (hash secret-0 challenge-0 id-0)))
      (send (enc challenge-0 (hash secret-0 challenge-0 id-0)))
      (recv request-0) (init request-0) (obsv (cat secret cookie-0))
      (send
        (cat secret
          (enc cookie-0
            (hash syskey-0 (hash secret-0 challenge-0 id-0)))))))
  (label 8)
  (parent 2)
  (unrealized (0 6) (2 7))
  (dead)
  (comment "empty cohort"))

(defskeleton sbp
  (vars (challenge secret id syskey request answer cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (deflistener (hash secret challenge id))
  (deflistener (cat secret challenge id))
  (precedes ((0 2) (2 0)) ((1 1) (0 4)) ((2 1) (1 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation encryption-test (added-listener (cat secret challenge id))
    (hash secret challenge id) (1 0))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv (hash secret challenge id))
      (send (hash secret challenge id)))
    ((recv (cat secret challenge id)) (send (cat secret challenge id))))
  (label 9)
  (parent 3)
  (unrealized (0 6) (2 0))
  (comment "2 in cohort - 2 not yet seen"))

(defskeleton sbp
  (vars (secret id syskey request answer cookie request-0 answer-0 data)
    (proxy name))
  (defstrand client 7 (challenge id) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((1 8) (0 6)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig secret id)
  (operation nonce-test (contracted (proxy-0 proxy)) secret (1 2)
    (enc secret (pubk proxy)))
  (traces
    ((send id) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret id id)))
      (recv (enc id (hash secret id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash secret id id))))))
    ((recv id) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret id id)))
      (send (enc id (hash secret id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash secret id id)))))))
  (label 10)
  (parent 4)
  (unrealized (1 3) (1 7))
  (comment "2 in cohort - 2 not yet seen"))

(defskeleton sbp
  (vars (id syskey request answer cookie request-0 answer-0 data)
    (proxy proxy-0 name))
  (defstrand client 7 (challenge id) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 5) (1 2)) ((1 8) (0 6)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig id request)
  (operation nonce-test (displaced 2 0 client 6) secret (1 2)
    (enc secret (pubk proxy)))
  (traces
    ((send id) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request id id)))
      (recv (enc id (hash request id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash request id id))))))
    ((recv id) (send id) (recv (enc request (pubk proxy-0)))
      (recv (enc id (hash request id id)))
      (send (enc id (hash request id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request id id)))))))
  (label 11)
  (parent 4)
  (unrealized (1 7))
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (secret id syskey request answer cookie request-0 answer-0 challenge
      id-0 secret-0 request-1 answer-1 syskey-0 data)
    (proxy proxy-0 proxy-1 name))
  (defstrand client 7 (challenge id) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy-0))
  (defstrand proxy 9 (challenge challenge) (id id-0) (secret secret-0)
    (request request-1) (answer answer-1) (cookie secret)
    (syskey syskey-0) (proxy proxy-1))
  (precedes ((0 0) (1 0)) ((0 2) (2 7)) ((1 8) (0 6)) ((2 8) (1 2)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig secret id)
  (operation nonce-test (added-strand proxy 9) secret (1 2)
    (enc secret (pubk proxy)))
  (traces
    ((send id) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret id id)))
      (recv (enc id (hash secret id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash secret id id))))))
    ((recv id) (send id) (recv (enc secret (pubk proxy-0)))
      (recv (enc id (hash secret id id)))
      (send (enc id (hash secret id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash secret id id))))))
    ((recv challenge) (send id-0) (recv (enc secret-0 (pubk proxy-1)))
      (recv (enc id-0 (hash secret-0 challenge id-0)))
      (send (enc challenge (hash secret-0 challenge id-0)))
      (recv request-1) (init request-1) (obsv (cat answer-1 secret))
      (send
        (cat answer-1
          (enc secret
            (hash syskey-0 (hash secret-0 challenge id-0)))))))
  (label 12)
  (parent 4)
  (unrealized (1 7) (2 7))
  (dead)
  (comment "empty cohort"))

(defskeleton sbp
  (vars
    (secret id syskey request answer cookie request-0 answer-0 challenge
      id-0 secret-0 request-1 cookie-0 syskey-0 data)
    (proxy proxy-0 proxy-1 name))
  (defstrand client 7 (challenge id) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy-0))
  (defstrand proxy 9 (challenge challenge) (id id-0) (secret secret-0)
    (request request-1) (answer secret) (cookie cookie-0)
    (syskey syskey-0) (proxy proxy-1))
  (precedes ((0 0) (1 0)) ((0 2) (2 7)) ((1 8) (0 6)) ((2 8) (1 2)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig secret id)
  (operation nonce-test (added-strand proxy 9) secret (1 2)
    (enc secret (pubk proxy)))
  (traces
    ((send id) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret id id)))
      (recv (enc id (hash secret id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash secret id id))))))
    ((recv id) (send id) (recv (enc secret (pubk proxy-0)))
      (recv (enc id (hash secret id id)))
      (send (enc id (hash secret id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash secret id id))))))
    ((recv challenge) (send id-0) (recv (enc secret-0 (pubk proxy-1)))
      (recv (enc id-0 (hash secret-0 challenge id-0)))
      (send (enc challenge (hash secret-0 challenge id-0)))
      (recv request-1) (init request-1) (obsv (cat secret cookie-0))
      (send
        (cat secret
          (enc cookie-0
            (hash syskey-0 (hash secret-0 challenge id-0)))))))
  (label 13)
  (parent 4)
  (unrealized (1 7) (2 7))
  (dead)
  (comment "empty cohort"))

(defskeleton sbp
  (vars (challenge secret id syskey request answer cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret secret)
    (proxy proxy))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 3) (1 3)) ((1 4) (0 4)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation encryption-test (displaced 2 0 client 4)
    (enc id (hash secret challenge id)) (1 3))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id)))))
  (label 14)
  (parent 6)
  (unrealized (0 6))
  (comment "3 in cohort - 3 not yet seen"))

(defskeleton sbp
  (vars (challenge secret id syskey request answer cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret secret)
    (proxy proxy))
  (deflistener (hash secret challenge id))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((1 4) (0 4)) ((2 1) (1 3)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation encryption-test (added-listener (hash secret challenge id))
    (enc id (hash secret challenge id)) (1 3))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))))
    ((recv (hash secret challenge id))
      (send (hash secret challenge id))))
  (label 15)
  (parent 6)
  (unrealized (0 6) (2 0))
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge secret id syskey request answer cookie challenge-0 id-0
      secret-0 request-0 answer-0 syskey-0 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (deflistener (hash secret challenge id))
  (deflistener (cat secret challenge id))
  (defstrand proxy 9 (challenge challenge-0) (id id-0) (secret secret-0)
    (request request-0) (answer answer-0) (cookie secret)
    (syskey syskey-0) (proxy proxy-0))
  (precedes ((0 2) (3 7)) ((1 1) (0 4)) ((2 1) (1 0)) ((3 8) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation nonce-test (added-strand proxy 9) secret (2 0)
    (enc secret (pubk proxy)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv (hash secret challenge id))
      (send (hash secret challenge id)))
    ((recv (cat secret challenge id)) (send (cat secret challenge id)))
    ((recv challenge-0) (send id-0) (recv (enc secret-0 (pubk proxy-0)))
      (recv (enc id-0 (hash secret-0 challenge-0 id-0)))
      (send (enc challenge-0 (hash secret-0 challenge-0 id-0)))
      (recv request-0) (init request-0) (obsv (cat answer-0 secret))
      (send
        (cat answer-0
          (enc secret
            (hash syskey-0 (hash secret-0 challenge-0 id-0)))))))
  (label 16)
  (parent 9)
  (unrealized (0 6) (3 7))
  (dead)
  (comment "empty cohort"))

(defskeleton sbp
  (vars
    (challenge secret id syskey request answer cookie challenge-0 id-0
      secret-0 request-0 cookie-0 syskey-0 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (deflistener (hash secret challenge id))
  (deflistener (cat secret challenge id))
  (defstrand proxy 9 (challenge challenge-0) (id id-0) (secret secret-0)
    (request request-0) (answer secret) (cookie cookie-0)
    (syskey syskey-0) (proxy proxy-0))
  (precedes ((0 2) (3 7)) ((1 1) (0 4)) ((2 1) (1 0)) ((3 8) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation nonce-test (added-strand proxy 9) secret (2 0)
    (enc secret (pubk proxy)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv (hash secret challenge id))
      (send (hash secret challenge id)))
    ((recv (cat secret challenge id)) (send (cat secret challenge id)))
    ((recv challenge-0) (send id-0) (recv (enc secret-0 (pubk proxy-0)))
      (recv (enc id-0 (hash secret-0 challenge-0 id-0)))
      (send (enc challenge-0 (hash secret-0 challenge-0 id-0)))
      (recv request-0) (init request-0) (obsv (cat secret cookie-0))
      (send
        (cat secret
          (enc cookie-0
            (hash syskey-0 (hash secret-0 challenge-0 id-0)))))))
  (label 17)
  (parent 9)
  (unrealized (0 6) (3 7))
  (dead)
  (comment "empty cohort"))

(defskeleton sbp
  (vars (secret id syskey request answer cookie request-0 answer-0 data)
    (proxy name))
  (defstrand client 7 (challenge id) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 3) (1 3)) ((1 8) (0 6)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig secret id)
  (operation encryption-test (displaced 2 0 client 4)
    (enc id (hash secret id id)) (1 3))
  (traces
    ((send id) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret id id)))
      (recv (enc id (hash secret id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash secret id id))))))
    ((recv id) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret id id)))
      (send (enc id (hash secret id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash secret id id)))))))
  (label 18)
  (parent 10)
  (unrealized (1 7))
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (secret id syskey request answer cookie request-0 answer-0 data)
    (proxy name))
  (defstrand client 7 (challenge id) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (deflistener (hash secret id id))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((1 8) (0 6)) ((2 1) (1 3)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig secret id)
  (operation encryption-test (added-listener (hash secret id id))
    (enc id (hash secret id id)) (1 3))
  (traces
    ((send id) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret id id)))
      (recv (enc id (hash secret id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash secret id id))))))
    ((recv id) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret id id)))
      (send (enc id (hash secret id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash secret id id))))))
    ((recv (hash secret id id)) (send (hash secret id id))))
  (label 19)
  (parent 10)
  (unrealized (1 7) (2 0))
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (id syskey request answer request-0 request-1 answer-0 cookie data)
    (proxy proxy-0 name))
  (defstrand client 7 (challenge id) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy-0))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 5) (1 2)) ((1 8) (0 6)) ((2 0) (1 7)))
  (leadsto ((2 0) (1 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig id request)
  (operation state-passing-test (added-strand server 1)
    (cat answer-0 cookie) (1 7))
  (traces
    ((send id) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request id id)))
      (recv (enc id (hash request id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash request id id))))))
    ((recv id) (send id) (recv (enc request (pubk proxy-0)))
      (recv (enc id (hash request id id)))
      (send (enc id (hash request id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash request id id))))))
    ((tran request-1 (cat answer-0 cookie))))
  (label 20)
  (parent 11)
  (unrealized (2 0))
  (comment "2 in cohort - 2 not yet seen"))

(defskeleton sbp
  (vars
    (challenge secret id syskey request answer cookie request-0 answer-0
      data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 3) (1 3)) ((1 4) (0 4))
    ((1 8) (0 6)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation encryption-test (displaced 1 2 proxy 9)
    (enc cookie (hash syskey (hash secret challenge id))) (0 6))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id)))))))
  (label 21)
  (parent 14)
  (unrealized (1 7))
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge secret id syskey request answer cookie request-0 answer-0
      data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret secret)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((1 4) (0 4)) ((2 8) (0 6)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation encryption-test (added-strand proxy 9)
    (enc cookie (hash syskey (hash secret challenge id))) (0 6))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy-0)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id)))))))
  (label 22)
  (parent 14)
  (unrealized (2 2) (2 3) (2 7))
  (comment "6 in cohort - 6 not yet seen"))

(defskeleton sbp
  (vars (challenge secret id syskey request answer cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret secret)
    (proxy proxy))
  (deflistener (hash syskey (hash secret challenge id)))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 3) (1 3)) ((1 4) (0 4))
    ((2 1) (0 6)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation encryption-test
    (added-listener (hash syskey (hash secret challenge id)))
    (enc cookie (hash syskey (hash secret challenge id))) (0 6))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))))
    ((recv (hash syskey (hash secret challenge id)))
      (send (hash syskey (hash secret challenge id)))))
  (label 23)
  (parent 14)
  (unrealized (2 0))
  (dead)
  (comment "empty cohort"))

(defskeleton sbp
  (vars (challenge secret id syskey request answer cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret secret)
    (proxy proxy))
  (deflistener (hash secret challenge id))
  (deflistener (cat secret challenge id))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 2) (3 0)) ((1 4) (0 4))
    ((2 1) (1 3)) ((3 1) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation encryption-test (added-listener (cat secret challenge id))
    (hash secret challenge id) (2 0))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))))
    ((recv (hash secret challenge id))
      (send (hash secret challenge id)))
    ((recv (cat secret challenge id)) (send (cat secret challenge id))))
  (label 24)
  (parent 15)
  (unrealized (0 6) (3 0))
  (comment "2 in cohort - 2 not yet seen"))

(defskeleton sbp
  (vars
    (secret id syskey request answer request-0 request-1 answer-0 cookie
      data) (proxy name))
  (defstrand client 7 (challenge id) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 3) (1 3)) ((1 8) (0 6))
    ((2 0) (1 7)))
  (leadsto ((2 0) (1 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig secret id)
  (operation state-passing-test (added-strand server 1)
    (cat answer-0 cookie) (1 7))
  (traces
    ((send id) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret id id)))
      (recv (enc id (hash secret id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash secret id id))))))
    ((recv id) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret id id)))
      (send (enc id (hash secret id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash secret id id))))))
    ((tran request-1 (cat answer-0 cookie))))
  (label 25)
  (parent 18)
  (unrealized (2 0))
  (comment "2 in cohort - 2 not yet seen"))

(defskeleton sbp
  (vars (secret id syskey request answer cookie request-0 answer-0 data)
    (proxy name))
  (defstrand client 7 (challenge id) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (deflistener (hash secret id id))
  (deflistener (cat secret id id))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 2) (3 0)) ((1 8) (0 6))
    ((2 1) (1 3)) ((3 1) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig secret id)
  (operation encryption-test (added-listener (cat secret id id))
    (hash secret id id) (2 0))
  (traces
    ((send id) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret id id)))
      (recv (enc id (hash secret id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash secret id id))))))
    ((recv id) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret id id)))
      (send (enc id (hash secret id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash secret id id))))))
    ((recv (hash secret id id)) (send (hash secret id id)))
    ((recv (cat secret id id)) (send (cat secret id id))))
  (label 26)
  (parent 19)
  (unrealized (1 7) (3 0))
  (comment "3 in cohort - 3 not yet seen"))

(defskeleton sbp
  (vars (id syskey request answer request-0 answer-0 cookie data)
    (proxy proxy-0 name))
  (defstrand client 7 (challenge id) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy-0))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 5) (1 2)) ((1 6) (2 0)) ((1 8) (0 6))
    ((2 0) (1 7)))
  (leadsto ((1 6) (2 0)) ((2 0) (1 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig id request)
  (operation state-passing-test (displaced 3 1 proxy 7) request-1 (2 0))
  (traces
    ((send id) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request id id)))
      (recv (enc id (hash request id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash request id id))))))
    ((recv id) (send id) (recv (enc request (pubk proxy-0)))
      (recv (enc id (hash request id id)))
      (send (enc id (hash request id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash request id id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 27)
  (parent 20)
  (unrealized)
  (shape)
  (maps
    ((0)
      ((challenge id) (secret request) (id id) (syskey syskey)
        (proxy proxy) (request request) (answer answer)
        (cookie cookie))))
  (origs (id (0 0)) (request (0 2))))

(defskeleton sbp
  (vars
    (id syskey request answer request-0 answer-0 cookie challenge id-0
      secret request-1 data) (proxy proxy-0 proxy-1 name))
  (defstrand client 7 (challenge id) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy-0))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge) (id id-0) (secret secret)
    (request request-1) (proxy proxy-1))
  (precedes ((0 0) (1 0)) ((0 5) (1 2)) ((1 8) (0 6)) ((2 0) (1 7))
    ((3 6) (2 0)))
  (leadsto ((2 0) (1 7)) ((3 6) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig id request)
  (operation state-passing-test (added-strand proxy 7) request-1 (2 0))
  (traces
    ((send id) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request id id)))
      (recv (enc id (hash request id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash request id id))))))
    ((recv id) (send id) (recv (enc request (pubk proxy-0)))
      (recv (enc id (hash request id id)))
      (send (enc id (hash request id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash request id id))))))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge) (send id-0) (recv (enc secret (pubk proxy-1)))
      (recv (enc id-0 (hash secret challenge id-0)))
      (send (enc challenge (hash secret challenge id-0)))
      (recv request-1) (init request-1)))
  (label 28)
  (parent 20)
  (unrealized)
  (shape)
  (maps
    ((0)
      ((challenge id) (secret request) (id id) (syskey syskey)
        (proxy proxy) (request request) (answer answer)
        (cookie cookie))))
  (origs (id (0 0)) (request (0 2))))

(defskeleton sbp
  (vars
    (challenge secret id syskey request answer request-0 request-1
      answer-0 cookie data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 3) (1 3)) ((1 4) (0 4))
    ((1 8) (0 6)) ((2 0) (1 7)))
  (leadsto ((2 0) (1 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation state-passing-test (added-strand server 1)
    (cat answer-0 cookie) (1 7))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((tran request-1 (cat answer-0 cookie))))
  (label 29)
  (parent 21)
  (unrealized (2 0))
  (comment "2 in cohort - 2 not yet seen"))

(defskeleton sbp
  (vars
    (challenge secret id syskey request answer cookie request-0 answer-0
      data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret secret)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((1 4) (0 4)) ((2 8) (0 6)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation nonce-test (contracted (proxy-0 proxy)) secret (2 2)
    (enc secret (pubk proxy)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id)))))))
  (label 30)
  (parent 22)
  (unrealized (2 3) (2 7))
  (comment "2 in cohort - 2 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer cookie request-0 answer-0 data)
    (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 3) (1 3))
    ((0 5) (2 2)) ((1 4) (0 4)) ((2 8) (0 6)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation nonce-test (displaced 3 0 client 6) secret (2 2)
    (enc secret (pubk proxy)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy-0)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id)))))))
  (label 31)
  (parent 22)
  (unrealized (2 7))
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (syskey request answer cookie request-0 answer-0 challenge id secret
      request-1 answer-1 syskey-0 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy-0))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-1) (answer answer-1) (cookie secret)
    (syskey syskey-0) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (2 2)) ((0 3) (2 3))
    ((1 8) (0 6)) ((2 4) (0 4)) ((2 8) (1 2)))
  (non-orig syskey (privk proxy-0))
  (pen-non-orig (pubk proxy-0))
  (uniq-orig challenge secret)
  (operation nonce-test (displaced 1 3 proxy 9) secret-0 (2 2)
    (enc secret-0 (pubk proxy-1)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy-0)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy-0)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-1)
      (init request-1) (obsv (cat answer-1 secret))
      (send
        (cat answer-1
          (enc secret (hash syskey-0 (hash secret challenge id)))))))
  (label 32)
  (parent 22)
  (unrealized (1 2) (1 7) (2 7))
  (dead)
  (comment "empty cohort"))

(defskeleton sbp
  (vars
    (challenge secret id syskey request answer cookie request-0 answer-0
      challenge-0 id-0 secret-0 request-1 answer-1 syskey-0 data)
    (proxy proxy-0 proxy-1 name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret secret)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy-0))
  (defstrand proxy 9 (challenge challenge-0) (id id-0) (secret secret-0)
    (request request-1) (answer answer-1) (cookie secret)
    (syskey syskey-0) (proxy proxy-1))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (3 7))
    ((0 3) (1 3)) ((1 4) (0 4)) ((2 8) (0 6)) ((3 8) (2 2)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation nonce-test (added-strand proxy 9) secret (2 2)
    (enc secret (pubk proxy)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy-0)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge-0) (send id-0) (recv (enc secret-0 (pubk proxy-1)))
      (recv (enc id-0 (hash secret-0 challenge-0 id-0)))
      (send (enc challenge-0 (hash secret-0 challenge-0 id-0)))
      (recv request-1) (init request-1) (obsv (cat answer-1 secret))
      (send
        (cat answer-1
          (enc secret
            (hash syskey-0 (hash secret-0 challenge-0 id-0)))))))
  (label 33)
  (parent 22)
  (unrealized (2 7) (3 7))
  (dead)
  (comment "empty cohort"))

(defskeleton sbp
  (vars
    (syskey request answer cookie request-0 answer-0 challenge id secret
      request-1 cookie-0 syskey-0 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy-0))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-1) (answer secret) (cookie cookie-0)
    (syskey syskey-0) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (2 2)) ((0 3) (2 3))
    ((1 8) (0 6)) ((2 4) (0 4)) ((2 8) (1 2)))
  (non-orig syskey (privk proxy-0))
  (pen-non-orig (pubk proxy-0))
  (uniq-orig challenge secret)
  (operation nonce-test (displaced 1 3 proxy 9) secret-0 (2 2)
    (enc secret-0 (pubk proxy-1)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy-0)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy-0)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-1)
      (init request-1) (obsv (cat secret cookie-0))
      (send
        (cat secret
          (enc cookie-0 (hash syskey-0 (hash secret challenge id)))))))
  (label 34)
  (parent 22)
  (unrealized (1 7) (2 7))
  (dead)
  (comment "empty cohort"))

(defskeleton sbp
  (vars
    (challenge secret id syskey request answer cookie request-0 answer-0
      challenge-0 id-0 secret-0 request-1 cookie-0 syskey-0 data)
    (proxy proxy-0 proxy-1 name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret secret)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy-0))
  (defstrand proxy 9 (challenge challenge-0) (id id-0) (secret secret-0)
    (request request-1) (answer secret) (cookie cookie-0)
    (syskey syskey-0) (proxy proxy-1))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (3 7))
    ((0 3) (1 3)) ((1 4) (0 4)) ((2 8) (0 6)) ((3 8) (2 2)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation nonce-test (added-strand proxy 9) secret (2 2)
    (enc secret (pubk proxy)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy-0)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge-0) (send id-0) (recv (enc secret-0 (pubk proxy-1)))
      (recv (enc id-0 (hash secret-0 challenge-0 id-0)))
      (send (enc challenge-0 (hash secret-0 challenge-0 id-0)))
      (recv request-1) (init request-1) (obsv (cat secret cookie-0))
      (send
        (cat secret
          (enc cookie-0
            (hash syskey-0 (hash secret-0 challenge-0 id-0)))))))
  (label 35)
  (parent 22)
  (unrealized (2 7) (3 7))
  (dead)
  (comment "empty cohort"))

(defskeleton sbp
  (vars
    (challenge secret id syskey request answer cookie challenge-0 id-0
      secret-0 request-0 answer-0 syskey-0 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret secret)
    (proxy proxy))
  (deflistener (hash secret challenge id))
  (deflistener (cat secret challenge id))
  (defstrand proxy 9 (challenge challenge-0) (id id-0) (secret secret-0)
    (request request-0) (answer answer-0) (cookie secret)
    (syskey syskey-0) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 2) (4 7)) ((1 4) (0 4))
    ((2 1) (1 3)) ((3 1) (2 0)) ((4 8) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation nonce-test (added-strand proxy 9) secret (3 0)
    (enc secret (pubk proxy)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))))
    ((recv (hash secret challenge id))
      (send (hash secret challenge id)))
    ((recv (cat secret challenge id)) (send (cat secret challenge id)))
    ((recv challenge-0) (send id-0) (recv (enc secret-0 (pubk proxy-0)))
      (recv (enc id-0 (hash secret-0 challenge-0 id-0)))
      (send (enc challenge-0 (hash secret-0 challenge-0 id-0)))
      (recv request-0) (init request-0) (obsv (cat answer-0 secret))
      (send
        (cat answer-0
          (enc secret
            (hash syskey-0 (hash secret-0 challenge-0 id-0)))))))
  (label 36)
  (parent 24)
  (unrealized (0 6) (4 7))
  (dead)
  (comment "empty cohort"))

(defskeleton sbp
  (vars
    (challenge secret id syskey request answer cookie challenge-0 id-0
      secret-0 request-0 cookie-0 syskey-0 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret secret)
    (proxy proxy))
  (deflistener (hash secret challenge id))
  (deflistener (cat secret challenge id))
  (defstrand proxy 9 (challenge challenge-0) (id id-0) (secret secret-0)
    (request request-0) (answer secret) (cookie cookie-0)
    (syskey syskey-0) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 2) (4 7)) ((1 4) (0 4))
    ((2 1) (1 3)) ((3 1) (2 0)) ((4 8) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation nonce-test (added-strand proxy 9) secret (3 0)
    (enc secret (pubk proxy)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))))
    ((recv (hash secret challenge id))
      (send (hash secret challenge id)))
    ((recv (cat secret challenge id)) (send (cat secret challenge id)))
    ((recv challenge-0) (send id-0) (recv (enc secret-0 (pubk proxy-0)))
      (recv (enc id-0 (hash secret-0 challenge-0 id-0)))
      (send (enc challenge-0 (hash secret-0 challenge-0 id-0)))
      (recv request-0) (init request-0) (obsv (cat secret cookie-0))
      (send
        (cat secret
          (enc cookie-0
            (hash syskey-0 (hash secret-0 challenge-0 id-0)))))))
  (label 37)
  (parent 24)
  (unrealized (0 6) (4 7))
  (dead)
  (comment "empty cohort"))

(defskeleton sbp
  (vars (secret id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge id) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 3) (1 3)) ((1 6) (2 0))
    ((1 8) (0 6)) ((2 0) (1 7)))
  (leadsto ((1 6) (2 0)) ((2 0) (1 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig secret id)
  (operation state-passing-test (displaced 3 1 proxy 7) request-1 (2 0))
  (traces
    ((send id) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret id id)))
      (recv (enc id (hash secret id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash secret id id))))))
    ((recv id) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret id id)))
      (send (enc id (hash secret id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash secret id id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 38)
  (parent 25)
  (unrealized)
  (shape)
  (maps
    ((0)
      ((challenge id) (secret secret) (id id) (syskey syskey)
        (proxy proxy) (request request) (answer answer)
        (cookie cookie))))
  (origs (id (0 0)) (secret (0 2))))

(defskeleton sbp
  (vars
    (secret id syskey request answer request-0 answer-0 cookie challenge
      id-0 secret-0 request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge id) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge) (id id-0) (secret secret-0)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 3) (1 3)) ((1 8) (0 6))
    ((2 0) (1 7)) ((3 6) (2 0)))
  (leadsto ((2 0) (1 7)) ((3 6) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig secret id)
  (operation state-passing-test (added-strand proxy 7) request-1 (2 0))
  (traces
    ((send id) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret id id)))
      (recv (enc id (hash secret id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash secret id id))))))
    ((recv id) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret id id)))
      (send (enc id (hash secret id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash secret id id))))))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge) (send id-0) (recv (enc secret-0 (pubk proxy-0)))
      (recv (enc id-0 (hash secret-0 challenge id-0)))
      (send (enc challenge (hash secret-0 challenge id-0)))
      (recv request-1) (init request-1)))
  (label 39)
  (parent 25)
  (unrealized)
  (shape)
  (maps
    ((0)
      ((challenge id) (secret secret) (id id) (syskey syskey)
        (proxy proxy) (request request) (answer answer)
        (cookie cookie))))
  (origs (id (0 0)) (secret (0 2))))

(defskeleton sbp
  (vars (id syskey request answer cookie request-0 answer-0 data)
    (proxy name))
  (defstrand client 7 (challenge id) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (deflistener (hash request id id))
  (deflistener (cat request id id))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 5) (3 0)) ((1 8) (0 6))
    ((2 1) (1 3)) ((3 1) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig id request)
  (operation nonce-test (displaced 4 0 client 6) secret (3 0)
    (enc secret (pubk proxy)))
  (traces
    ((send id) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request id id)))
      (recv (enc id (hash request id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash request id id))))))
    ((recv id) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request id id)))
      (send (enc id (hash request id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash request id id))))))
    ((recv (hash request id id)) (send (hash request id id)))
    ((recv (cat request id id)) (send (cat request id id))))
  (label 40)
  (parent 26)
  (unrealized (1 7))
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (secret id syskey request answer cookie request-0 answer-0 challenge
      id-0 secret-0 request-1 answer-1 syskey-0 data)
    (proxy proxy-0 name))
  (defstrand client 7 (challenge id) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (deflistener (hash secret id id))
  (deflistener (cat secret id id))
  (defstrand proxy 9 (challenge challenge) (id id-0) (secret secret-0)
    (request request-1) (answer answer-1) (cookie secret)
    (syskey syskey-0) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 2) (4 7)) ((1 8) (0 6))
    ((2 1) (1 3)) ((3 1) (2 0)) ((4 8) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig secret id)
  (operation nonce-test (added-strand proxy 9) secret (3 0)
    (enc secret (pubk proxy)))
  (traces
    ((send id) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret id id)))
      (recv (enc id (hash secret id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash secret id id))))))
    ((recv id) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret id id)))
      (send (enc id (hash secret id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash secret id id))))))
    ((recv (hash secret id id)) (send (hash secret id id)))
    ((recv (cat secret id id)) (send (cat secret id id)))
    ((recv challenge) (send id-0) (recv (enc secret-0 (pubk proxy-0)))
      (recv (enc id-0 (hash secret-0 challenge id-0)))
      (send (enc challenge (hash secret-0 challenge id-0)))
      (recv request-1) (init request-1) (obsv (cat answer-1 secret))
      (send
        (cat answer-1
          (enc secret
            (hash syskey-0 (hash secret-0 challenge id-0)))))))
  (label 41)
  (parent 26)
  (unrealized (1 7) (4 7))
  (dead)
  (comment "empty cohort"))

(defskeleton sbp
  (vars
    (secret id syskey request answer cookie request-0 answer-0 challenge
      id-0 secret-0 request-1 cookie-0 syskey-0 data)
    (proxy proxy-0 name))
  (defstrand client 7 (challenge id) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (deflistener (hash secret id id))
  (deflistener (cat secret id id))
  (defstrand proxy 9 (challenge challenge) (id id-0) (secret secret-0)
    (request request-1) (answer secret) (cookie cookie-0)
    (syskey syskey-0) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 2) (4 7)) ((1 8) (0 6))
    ((2 1) (1 3)) ((3 1) (2 0)) ((4 8) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig secret id)
  (operation nonce-test (added-strand proxy 9) secret (3 0)
    (enc secret (pubk proxy)))
  (traces
    ((send id) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret id id)))
      (recv (enc id (hash secret id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash secret id id))))))
    ((recv id) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret id id)))
      (send (enc id (hash secret id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash secret id id))))))
    ((recv (hash secret id id)) (send (hash secret id id)))
    ((recv (cat secret id id)) (send (cat secret id id)))
    ((recv challenge) (send id-0) (recv (enc secret-0 (pubk proxy-0)))
      (recv (enc id-0 (hash secret-0 challenge id-0)))
      (send (enc challenge (hash secret-0 challenge id-0)))
      (recv request-1) (init request-1) (obsv (cat secret cookie-0))
      (send
        (cat secret
          (enc cookie-0
            (hash syskey-0 (hash secret-0 challenge id-0)))))))
  (label 42)
  (parent 26)
  (unrealized (1 7) (4 7))
  (dead)
  (comment "empty cohort"))

(defskeleton sbp
  (vars
    (challenge secret id syskey request answer request-0 answer-0 cookie
      data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 3) (1 3)) ((1 4) (0 4))
    ((1 6) (2 0)) ((1 8) (0 6)) ((2 0) (1 7)))
  (leadsto ((1 6) (2 0)) ((2 0) (1 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation state-passing-test (displaced 3 1 proxy 7) request-1 (2 0))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 43)
  (parent 29)
  (unrealized)
  (shape)
  (maps
    ((0)
      ((challenge challenge) (secret secret) (id id) (syskey syskey)
        (proxy proxy) (request request) (answer answer)
        (cookie cookie))))
  (origs (challenge (0 0)) (secret (0 2))))

(defskeleton sbp
  (vars
    (challenge secret id syskey request answer request-0 answer-0 cookie
      challenge-0 id-0 secret-0 request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge-0) (id id-0) (secret secret-0)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 3) (1 3)) ((1 4) (0 4))
    ((1 8) (0 6)) ((2 0) (1 7)) ((3 6) (2 0)))
  (leadsto ((2 0) (1 7)) ((3 6) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation state-passing-test (added-strand proxy 7) request-1 (2 0))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge-0) (send id-0) (recv (enc secret-0 (pubk proxy-0)))
      (recv (enc id-0 (hash secret-0 challenge-0 id-0)))
      (send (enc challenge-0 (hash secret-0 challenge-0 id-0)))
      (recv request-1) (init request-1)))
  (label 44)
  (parent 29)
  (unrealized)
  (shape)
  (maps
    ((0)
      ((challenge challenge) (secret secret) (id id) (syskey syskey)
        (proxy proxy) (request request) (answer answer)
        (cookie cookie))))
  (origs (challenge (0 0)) (secret (0 2))))

(defskeleton sbp
  (vars
    (challenge secret id syskey request answer cookie request-0 answer-0
      data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret secret)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((1 4) (0 4)) ((2 8) (0 6)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation encryption-test (displaced 3 0 client 4)
    (enc id (hash secret challenge id)) (2 3))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id)))))))
  (label 45)
  (parent 30)
  (unrealized (2 7))
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge secret id syskey request answer cookie request-0 answer-0
      data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret secret)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (deflistener (hash secret challenge id))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((1 4) (0 4)) ((2 8) (0 6)) ((3 1) (2 3)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation encryption-test (added-listener (hash secret challenge id))
    (enc id (hash secret challenge id)) (2 3))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv (hash secret challenge id))
      (send (hash secret challenge id))))
  (label 46)
  (parent 30)
  (unrealized (2 7) (3 0))
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 request-1 answer-0
      cookie data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy-0))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 3) (1 3))
    ((0 5) (2 2)) ((1 4) (0 4)) ((2 8) (0 6)) ((3 0) (2 7)))
  (leadsto ((3 0) (2 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation state-passing-test (added-strand server 1)
    (cat answer-0 cookie) (2 7))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy-0)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-1 (cat answer-0 cookie))))
  (label 47)
  (parent 31)
  (unrealized (3 0))
  (comment "3 in cohort - 3 not yet seen"))

(defskeleton sbp
  (vars
    (id syskey request answer request-0 request-1 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge id) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (deflistener (hash request id id))
  (deflistener (cat request id id))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 5) (3 0)) ((1 8) (0 6))
    ((2 1) (1 3)) ((3 1) (2 0)) ((4 0) (1 7)))
  (leadsto ((4 0) (1 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig id request)
  (operation state-passing-test (added-strand server 1)
    (cat answer-0 cookie) (1 7))
  (traces
    ((send id) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request id id)))
      (recv (enc id (hash request id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash request id id))))))
    ((recv id) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request id id)))
      (send (enc id (hash request id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash request id id))))))
    ((recv (hash request id id)) (send (hash request id id)))
    ((recv (cat request id id)) (send (cat request id id)))
    ((tran request-1 (cat answer-0 cookie))))
  (label 48)
  (parent 40)
  (unrealized (4 0))
  (comment "2 in cohort - 2 not yet seen"))

(defskeleton sbp
  (vars
    (challenge secret id syskey request answer request-0 request-1
      answer-0 cookie data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret secret)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((1 4) (0 4)) ((2 8) (0 6))
    ((3 0) (2 7)))
  (leadsto ((3 0) (2 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation state-passing-test (added-strand server 1)
    (cat answer-0 cookie) (2 7))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((tran request-1 (cat answer-0 cookie))))
  (label 49)
  (parent 45)
  (unrealized (3 0))
  (comment "3 in cohort - 3 not yet seen"))

(defskeleton sbp
  (vars
    (challenge secret id syskey request answer cookie request-0 answer-0
      data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret secret)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (deflistener (hash secret challenge id))
  (deflistener (cat secret challenge id))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 2) (4 0)) ((0 3) (1 3)) ((1 4) (0 4)) ((2 8) (0 6))
    ((3 1) (2 3)) ((4 1) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation encryption-test (added-listener (cat secret challenge id))
    (hash secret challenge id) (3 0))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv (hash secret challenge id))
      (send (hash secret challenge id)))
    ((recv (cat secret challenge id)) (send (cat secret challenge id))))
  (label 50)
  (parent 46)
  (unrealized (2 7) (4 0))
  (comment "5 in cohort - 5 not yet seen"))

(defskeleton sbp
  (vars
    (syskey answer request answer-0 cookie challenge id secret request-0
      data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request secret) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy-0))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer-0) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge) (id id) (secret secret)
    (request request-0) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 0) (3 0)) ((0 2) (3 2)) ((0 3) (3 3))
    ((0 5) (1 2)) ((1 8) (0 6)) ((2 0) (1 7)) ((3 4) (0 4))
    ((3 6) (2 0)))
  (leadsto ((2 0) (1 7)) ((3 6) (2 0)))
  (non-orig syskey (privk proxy-0))
  (pen-non-orig (pubk proxy-0))
  (uniq-orig challenge secret)
  (operation state-passing-test (displaced 1 4 proxy 7) request-0 (3 0))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy-0)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send secret)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request)
      (init request) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((tran request-0 (cat answer-0 cookie)))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy-0)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0)))
  (label 51)
  (parent 47)
  (unrealized)
  (shape)
  (maps
    ((0)
      ((challenge challenge) (secret secret) (id id) (syskey syskey)
        (proxy proxy-0) (request secret) (answer answer)
        (cookie cookie))))
  (origs (challenge (0 0)) (secret (0 2))))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie data)
    (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy-0))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 3) (1 3))
    ((0 5) (2 2)) ((1 4) (0 4)) ((2 6) (3 0)) ((2 8) (0 6))
    ((3 0) (2 7)))
  (leadsto ((2 6) (3 0)) ((3 0) (2 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation state-passing-test (displaced 4 2 proxy 7) request-1 (3 0))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy-0)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 52)
  (parent 47)
  (unrealized)
  (shape)
  (maps
    ((0)
      ((challenge challenge) (secret request) (id id) (syskey syskey)
        (proxy proxy) (request request) (answer answer)
        (cookie cookie))))
  (origs (challenge (0 0)) (request (0 2))))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie
      challenge-0 id-0 secret request-1 data)
    (proxy proxy-0 proxy-1 name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy-0))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge-0) (id id-0) (secret secret)
    (request request-1) (proxy proxy-1))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 3) (1 3))
    ((0 5) (2 2)) ((1 4) (0 4)) ((2 8) (0 6)) ((3 0) (2 7))
    ((4 6) (3 0)))
  (leadsto ((3 0) (2 7)) ((4 6) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation state-passing-test (added-strand proxy 7) request-1 (3 0))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy-0)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge-0) (send id-0) (recv (enc secret (pubk proxy-1)))
      (recv (enc id-0 (hash secret challenge-0 id-0)))
      (send (enc challenge-0 (hash secret challenge-0 id-0)))
      (recv request-1) (init request-1)))
  (label 53)
  (parent 47)
  (unrealized)
  (shape)
  (maps
    ((0)
      ((challenge challenge) (secret request) (id id) (syskey syskey)
        (proxy proxy) (request request) (answer answer)
        (cookie cookie))))
  (origs (challenge (0 0)) (request (0 2))))

(defskeleton sbp
  (vars (id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge id) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (deflistener (hash request id id))
  (deflistener (cat request id id))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 5) (3 0)) ((1 6) (4 0))
    ((1 8) (0 6)) ((2 1) (1 3)) ((3 1) (2 0)) ((4 0) (1 7)))
  (leadsto ((1 6) (4 0)) ((4 0) (1 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig id request)
  (operation state-passing-test (displaced 5 1 proxy 7) request-1 (4 0))
  (traces
    ((send id) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request id id)))
      (recv (enc id (hash request id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash request id id))))))
    ((recv id) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request id id)))
      (send (enc id (hash request id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash request id id))))))
    ((recv (hash request id id)) (send (hash request id id)))
    ((recv (cat request id id)) (send (cat request id id)))
    ((tran request-0 (cat answer-0 cookie))))
  (label 54)
  (parent 48)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (id syskey request answer request-0 answer-0 cookie challenge id-0
      secret request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge id) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (deflistener (hash request id id))
  (deflistener (cat request id id))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge) (id id-0) (secret secret)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 5) (3 0)) ((1 8) (0 6))
    ((2 1) (1 3)) ((3 1) (2 0)) ((4 0) (1 7)) ((5 6) (4 0)))
  (leadsto ((4 0) (1 7)) ((5 6) (4 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig id request)
  (operation state-passing-test (added-strand proxy 7) request-1 (4 0))
  (traces
    ((send id) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request id id)))
      (recv (enc id (hash request id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash request id id))))))
    ((recv id) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request id id)))
      (send (enc id (hash request id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash request id id))))))
    ((recv (hash request id id)) (send (hash request id id)))
    ((recv (cat request id id)) (send (cat request id id)))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge) (send id-0) (recv (enc secret (pubk proxy-0)))
      (recv (enc id-0 (hash secret challenge id-0)))
      (send (enc challenge (hash secret challenge id-0)))
      (recv request-1) (init request-1)))
  (label 55)
  (parent 48)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (syskey request answer request-0 answer-0 cookie challenge id secret
      request-1 data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge) (id id) (secret secret)
    (request request-1) (proxy proxy))
  (precedes ((0 0) (1 0)) ((0 0) (3 0)) ((0 2) (1 2)) ((0 2) (3 2))
    ((0 3) (1 3)) ((0 3) (3 3)) ((1 8) (0 6)) ((2 0) (1 7))
    ((3 4) (0 4)) ((3 6) (2 0)))
  (leadsto ((2 0) (1 7)) ((3 6) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation state-passing-test (displaced 1 4 proxy 7) request-1 (3 0))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-1)
      (init request-1)))
  (label 56)
  (parent 49)
  (unrealized)
  (shape)
  (maps
    ((0)
      ((challenge challenge) (secret secret) (id id) (syskey syskey)
        (proxy proxy) (request request) (answer answer)
        (cookie cookie))))
  (origs (challenge (0 0)) (secret (0 2))))

(defskeleton sbp
  (vars
    (challenge secret id syskey request answer request-0 answer-0 cookie
      data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret secret)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((1 4) (0 4)) ((2 6) (3 0))
    ((2 8) (0 6)) ((3 0) (2 7)))
  (leadsto ((2 6) (3 0)) ((3 0) (2 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation state-passing-test (displaced 4 2 proxy 7) request-1 (3 0))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 57)
  (parent 49)
  (unrealized)
  (shape)
  (maps
    ((0)
      ((challenge challenge) (secret secret) (id id) (syskey syskey)
        (proxy proxy) (request request) (answer answer)
        (cookie cookie))))
  (origs (challenge (0 0)) (secret (0 2))))

(defskeleton sbp
  (vars
    (challenge secret id syskey request answer request-0 answer-0 cookie
      challenge-0 id-0 secret-0 request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret secret)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge-0) (id id-0) (secret secret-0)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((1 4) (0 4)) ((2 8) (0 6))
    ((3 0) (2 7)) ((4 6) (3 0)))
  (leadsto ((3 0) (2 7)) ((4 6) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation state-passing-test (added-strand proxy 7) request-1 (3 0))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge-0) (send id-0) (recv (enc secret-0 (pubk proxy-0)))
      (recv (enc id-0 (hash secret-0 challenge-0 id-0)))
      (send (enc challenge-0 (hash secret-0 challenge-0 id-0)))
      (recv request-1) (init request-1)))
  (label 58)
  (parent 49)
  (unrealized)
  (shape)
  (maps
    ((0)
      ((challenge challenge) (secret secret) (id id) (syskey syskey)
        (proxy proxy) (request request) (answer answer)
        (cookie cookie))))
  (origs (challenge (0 0)) (secret (0 2))))

(defskeleton sbp
  (vars
    (challenge id syskey request answer cookie request-0 answer-0 data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (deflistener (hash request challenge id))
  (deflistener (cat request challenge id))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 5) (4 0)) ((1 4) (0 4)) ((2 8) (0 6))
    ((3 1) (2 3)) ((4 1) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation nonce-test (displaced 5 0 client 6) secret (4 0)
    (enc secret (pubk proxy)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv (hash request challenge id))
      (send (hash request challenge id)))
    ((recv (cat request challenge id))
      (send (cat request challenge id))))
  (label 59)
  (parent 50)
  (unrealized (2 7))
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (syskey request answer cookie request-0 answer-0 challenge id secret
      request-1 answer-1 syskey-0 data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (deflistener (hash secret challenge id))
  (deflistener (cat secret challenge id))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-1) (answer answer-1) (cookie secret)
    (syskey syskey-0) (proxy proxy))
  (precedes ((0 0) (1 0)) ((0 0) (4 0)) ((0 2) (1 2)) ((0 2) (4 2))
    ((0 3) (4 3)) ((1 8) (0 6)) ((2 1) (1 3)) ((3 1) (2 0))
    ((4 4) (0 4)) ((4 8) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation nonce-test (displaced 1 5 proxy 9) secret-0 (4 0)
    (enc secret-0 (pubk proxy-0)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv (hash secret challenge id))
      (send (hash secret challenge id)))
    ((recv (cat secret challenge id)) (send (cat secret challenge id)))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-1)
      (init request-1) (obsv (cat answer-1 secret))
      (send
        (cat answer-1
          (enc secret (hash syskey-0 (hash secret challenge id)))))))
  (label 60)
  (parent 50)
  (unrealized (1 7) (3 0) (4 7))
  (dead)
  (comment "empty cohort"))

(defskeleton sbp
  (vars
    (challenge secret id syskey request answer cookie request-0 answer-0
      challenge-0 id-0 secret-0 request-1 answer-1 syskey-0 data)
    (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret secret)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (deflistener (hash secret challenge id))
  (deflistener (cat secret challenge id))
  (defstrand proxy 9 (challenge challenge-0) (id id-0) (secret secret-0)
    (request request-1) (answer answer-1) (cookie secret)
    (syskey syskey-0) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 2) (5 7)) ((0 3) (1 3)) ((1 4) (0 4)) ((2 8) (0 6))
    ((3 1) (2 3)) ((4 1) (3 0)) ((5 8) (4 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation nonce-test (added-strand proxy 9) secret (4 0)
    (enc secret (pubk proxy)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv (hash secret challenge id))
      (send (hash secret challenge id)))
    ((recv (cat secret challenge id)) (send (cat secret challenge id)))
    ((recv challenge-0) (send id-0) (recv (enc secret-0 (pubk proxy-0)))
      (recv (enc id-0 (hash secret-0 challenge-0 id-0)))
      (send (enc challenge-0 (hash secret-0 challenge-0 id-0)))
      (recv request-1) (init request-1) (obsv (cat answer-1 secret))
      (send
        (cat answer-1
          (enc secret
            (hash syskey-0 (hash secret-0 challenge-0 id-0)))))))
  (label 61)
  (parent 50)
  (unrealized (2 7) (5 7))
  (dead)
  (comment "empty cohort"))

(defskeleton sbp
  (vars
    (syskey request answer cookie request-0 answer-0 challenge id secret
      request-1 cookie-0 syskey-0 data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (deflistener (hash secret challenge id))
  (deflistener (cat secret challenge id))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-1) (answer secret) (cookie cookie-0)
    (syskey syskey-0) (proxy proxy))
  (precedes ((0 0) (1 0)) ((0 0) (4 0)) ((0 2) (1 2)) ((0 2) (4 2))
    ((0 3) (4 3)) ((1 8) (0 6)) ((2 1) (1 3)) ((3 1) (2 0))
    ((4 4) (0 4)) ((4 8) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation nonce-test (displaced 1 5 proxy 9) secret-0 (4 0)
    (enc secret-0 (pubk proxy-0)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv (hash secret challenge id))
      (send (hash secret challenge id)))
    ((recv (cat secret challenge id)) (send (cat secret challenge id)))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-1)
      (init request-1) (obsv (cat secret cookie-0))
      (send
        (cat secret
          (enc cookie-0 (hash syskey-0 (hash secret challenge id)))))))
  (label 62)
  (parent 50)
  (unrealized (1 7) (4 7))
  (dead)
  (comment "empty cohort"))

(defskeleton sbp
  (vars
    (challenge secret id syskey request answer cookie request-0 answer-0
      challenge-0 id-0 secret-0 request-1 cookie-0 syskey-0 data)
    (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret secret)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (deflistener (hash secret challenge id))
  (deflistener (cat secret challenge id))
  (defstrand proxy 9 (challenge challenge-0) (id id-0) (secret secret-0)
    (request request-1) (answer secret) (cookie cookie-0)
    (syskey syskey-0) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 2) (5 7)) ((0 3) (1 3)) ((1 4) (0 4)) ((2 8) (0 6))
    ((3 1) (2 3)) ((4 1) (3 0)) ((5 8) (4 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation nonce-test (added-strand proxy 9) secret (4 0)
    (enc secret (pubk proxy)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv (hash secret challenge id))
      (send (hash secret challenge id)))
    ((recv (cat secret challenge id)) (send (cat secret challenge id)))
    ((recv challenge-0) (send id-0) (recv (enc secret-0 (pubk proxy-0)))
      (recv (enc id-0 (hash secret-0 challenge-0 id-0)))
      (send (enc challenge-0 (hash secret-0 challenge-0 id-0)))
      (recv request-1) (init request-1) (obsv (cat secret cookie-0))
      (send
        (cat secret
          (enc cookie-0
            (hash syskey-0 (hash secret-0 challenge-0 id-0)))))))
  (label 63)
  (parent 50)
  (unrealized (2 7) (5 7))
  (dead)
  (comment "empty cohort"))

(defskeleton sbp
  (vars (id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge id) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (deflistener (cat request id id))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 5) (2 0)) ((1 6) (3 0))
    ((1 8) (0 6)) ((2 1) (1 3)) ((3 0) (1 7)))
  (leadsto ((1 6) (3 0)) ((3 0) (1 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig id request)
  (operation generalization deleted (2 0))
  (traces
    ((send id) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request id id)))
      (recv (enc id (hash request id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash request id id))))))
    ((recv id) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request id id)))
      (send (enc id (hash request id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash request id id))))))
    ((recv (cat request id id)) (send (cat request id id)))
    ((tran request-0 (cat answer-0 cookie))))
  (label 64)
  (parent 54)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (id syskey request answer request-0 answer-0 cookie challenge id-0
      secret request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge id) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (deflistener (cat request id id))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge) (id id-0) (secret secret)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 5) (2 0)) ((1 8) (0 6))
    ((2 1) (1 3)) ((3 0) (1 7)) ((4 6) (3 0)))
  (leadsto ((3 0) (1 7)) ((4 6) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig id request)
  (operation generalization deleted (2 0))
  (traces
    ((send id) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request id id)))
      (recv (enc id (hash request id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash request id id))))))
    ((recv id) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request id id)))
      (send (enc id (hash request id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash request id id))))))
    ((recv (cat request id id)) (send (cat request id id)))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge) (send id-0) (recv (enc secret (pubk proxy-0)))
      (recv (enc id-0 (hash secret challenge id-0)))
      (send (enc challenge (hash secret challenge id-0)))
      (recv request-1) (init request-1)))
  (label 65)
  (parent 55)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 request-1 answer-0
      cookie data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (deflistener (hash request challenge id))
  (deflistener (cat request challenge id))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 5) (4 0)) ((1 4) (0 4)) ((2 8) (0 6))
    ((3 1) (2 3)) ((4 1) (3 0)) ((5 0) (2 7)))
  (leadsto ((5 0) (2 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation state-passing-test (added-strand server 1)
    (cat answer-0 cookie) (2 7))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv (hash request challenge id))
      (send (hash request challenge id)))
    ((recv (cat request challenge id))
      (send (cat request challenge id)))
    ((tran request-1 (cat answer-0 cookie))))
  (label 66)
  (parent 59)
  (unrealized (5 0))
  (comment "3 in cohort - 3 not yet seen"))

(defskeleton sbp
  (vars (id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge id) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 5) (1 3)) ((1 6) (2 0))
    ((1 8) (0 6)) ((2 0) (1 7)))
  (leadsto ((1 6) (2 0)) ((2 0) (1 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig id request)
  (operation generalization deleted (2 0))
  (traces
    ((send id) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request id id)))
      (recv (enc id (hash request id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash request id id))))))
    ((recv id) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request id id)))
      (send (enc id (hash request id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash request id id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 67)
  (parent 64)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (id syskey request answer request-0 answer-0 cookie challenge id-0
      secret request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge id) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge) (id id-0) (secret secret)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 5) (1 3)) ((1 8) (0 6))
    ((2 0) (1 7)) ((3 6) (2 0)))
  (leadsto ((2 0) (1 7)) ((3 6) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig id request)
  (operation generalization deleted (2 0))
  (traces
    ((send id) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request id id)))
      (recv (enc id (hash request id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash request id id))))))
    ((recv id) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request id id)))
      (send (enc id (hash request id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash request id id))))))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge) (send id-0) (recv (enc secret (pubk proxy-0)))
      (recv (enc id-0 (hash secret challenge id-0)))
      (send (enc challenge (hash secret challenge id-0)))
      (recv request-1) (init request-1)))
  (label 68)
  (parent 65)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (syskey answer request answer-0 cookie challenge id secret request-0
      data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request secret) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer-0) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (deflistener (hash secret challenge id))
  (deflistener (cat secret challenge id))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge) (id id) (secret secret)
    (request request-0) (proxy proxy))
  (precedes ((0 0) (1 0)) ((0 0) (5 0)) ((0 2) (1 2)) ((0 2) (5 2))
    ((0 3) (5 3)) ((0 5) (3 0)) ((1 8) (0 6)) ((2 1) (1 3))
    ((3 1) (2 0)) ((4 0) (1 7)) ((5 4) (0 4)) ((5 6) (4 0)))
  (leadsto ((4 0) (1 7)) ((5 6) (4 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation state-passing-test (displaced 1 6 proxy 7) request-0 (5 0))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send secret)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request)
      (init request) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv (hash secret challenge id))
      (send (hash secret challenge id)))
    ((recv (cat secret challenge id)) (send (cat secret challenge id)))
    ((tran request-0 (cat answer-0 cookie)))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0)))
  (label 69)
  (parent 66)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (deflistener (hash request challenge id))
  (deflistener (cat request challenge id))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 5) (4 0)) ((1 4) (0 4)) ((2 6) (5 0))
    ((2 8) (0 6)) ((3 1) (2 3)) ((4 1) (3 0)) ((5 0) (2 7)))
  (leadsto ((2 6) (5 0)) ((5 0) (2 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation state-passing-test (displaced 6 2 proxy 7) request-1 (5 0))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv (hash request challenge id))
      (send (hash request challenge id)))
    ((recv (cat request challenge id))
      (send (cat request challenge id)))
    ((tran request-0 (cat answer-0 cookie))))
  (label 70)
  (parent 66)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie
      challenge-0 id-0 secret request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (deflistener (hash request challenge id))
  (deflistener (cat request challenge id))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge-0) (id id-0) (secret secret)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 5) (4 0)) ((1 4) (0 4)) ((2 8) (0 6))
    ((3 1) (2 3)) ((4 1) (3 0)) ((5 0) (2 7)) ((6 6) (5 0)))
  (leadsto ((5 0) (2 7)) ((6 6) (5 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation state-passing-test (added-strand proxy 7) request-1 (5 0))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv (hash request challenge id))
      (send (hash request challenge id)))
    ((recv (cat request challenge id))
      (send (cat request challenge id)))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge-0) (send id-0) (recv (enc secret (pubk proxy-0)))
      (recv (enc id-0 (hash secret challenge-0 id-0)))
      (send (enc challenge-0 (hash secret challenge-0 id-0)))
      (recv request-1) (init request-1)))
  (label 71)
  (parent 66)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge id) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 3) (1 3)) ((0 5) (1 5))
    ((1 6) (2 0)) ((1 8) (0 6)) ((2 0) (1 7)))
  (leadsto ((1 6) (2 0)) ((2 0) (1 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig id request)
  (operation generalization weakened ((0 5) (1 3)))
  (traces
    ((send id) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request id id)))
      (recv (enc id (hash request id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash request id id))))))
    ((recv id) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request id id)))
      (send (enc id (hash request id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash request id id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 72)
  (parent 67)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (id syskey request answer request-0 answer-0 cookie challenge id-0
      secret request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge id) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge) (id id-0) (secret secret)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 3) (1 3)) ((0 5) (1 5))
    ((1 8) (0 6)) ((2 0) (1 7)) ((3 6) (2 0)))
  (leadsto ((2 0) (1 7)) ((3 6) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig id request)
  (operation generalization weakened ((0 5) (1 3)))
  (traces
    ((send id) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request id id)))
      (recv (enc id (hash request id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash request id id))))))
    ((recv id) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request id id)))
      (send (enc id (hash request id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash request id id))))))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge) (send id-0) (recv (enc secret (pubk proxy-0)))
      (recv (enc id-0 (hash secret challenge id-0)))
      (send (enc challenge (hash secret challenge id-0)))
      (recv request-1) (init request-1)))
  (label 73)
  (parent 68)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (syskey answer request answer-0 cookie challenge id secret request-0
      data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request secret) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer-0) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (deflistener (cat secret challenge id))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge) (id id) (secret secret)
    (request request-0) (proxy proxy))
  (precedes ((0 0) (1 0)) ((0 0) (4 0)) ((0 2) (1 2)) ((0 2) (4 2))
    ((0 3) (4 3)) ((0 5) (2 0)) ((1 8) (0 6)) ((2 1) (1 3))
    ((3 0) (1 7)) ((4 4) (0 4)) ((4 6) (3 0)))
  (leadsto ((3 0) (1 7)) ((4 6) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation generalization deleted (2 0))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send secret)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request)
      (init request) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv (cat secret challenge id)) (send (cat secret challenge id)))
    ((tran request-0 (cat answer-0 cookie)))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0)))
  (label 74)
  (parent 69)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (deflistener (cat request challenge id))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 5) (3 0)) ((1 4) (0 4)) ((2 6) (4 0))
    ((2 8) (0 6)) ((3 1) (2 3)) ((4 0) (2 7)))
  (leadsto ((2 6) (4 0)) ((4 0) (2 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization deleted (3 0))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv (cat request challenge id))
      (send (cat request challenge id)))
    ((tran request-0 (cat answer-0 cookie))))
  (label 75)
  (parent 70)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie
      challenge-0 id-0 secret request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (deflistener (cat request challenge id))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge-0) (id id-0) (secret secret)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 5) (3 0)) ((1 4) (0 4)) ((2 8) (0 6))
    ((3 1) (2 3)) ((4 0) (2 7)) ((5 6) (4 0)))
  (leadsto ((4 0) (2 7)) ((5 6) (4 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization deleted (3 0))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv (cat request challenge id))
      (send (cat request challenge id)))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge-0) (send id-0) (recv (enc secret (pubk proxy-0)))
      (recv (enc id-0 (hash secret challenge-0 id-0)))
      (send (enc challenge-0 (hash secret challenge-0 id-0)))
      (recv request-1) (init request-1)))
  (label 76)
  (parent 71)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge id) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 3) (1 3)) ((0 5) (1 6))
    ((1 6) (2 0)) ((1 8) (0 6)) ((2 0) (1 7)))
  (leadsto ((1 6) (2 0)) ((2 0) (1 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig id request)
  (operation generalization weakened ((0 5) (1 5)))
  (traces
    ((send id) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request id id)))
      (recv (enc id (hash request id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash request id id))))))
    ((recv id) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request id id)))
      (send (enc id (hash request id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash request id id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 77)
  (parent 72)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (id syskey request answer request-0 answer-0 cookie challenge id-0
      secret request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge id) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge) (id id-0) (secret secret)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 3) (1 3)) ((0 5) (1 6))
    ((1 8) (0 6)) ((2 0) (1 7)) ((3 6) (2 0)))
  (leadsto ((2 0) (1 7)) ((3 6) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig id request)
  (operation generalization weakened ((0 5) (1 5)))
  (traces
    ((send id) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request id id)))
      (recv (enc id (hash request id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash request id id))))))
    ((recv id) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request id id)))
      (send (enc id (hash request id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash request id id))))))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge) (send id-0) (recv (enc secret (pubk proxy-0)))
      (recv (enc id-0 (hash secret challenge id-0)))
      (send (enc challenge (hash secret challenge id-0)))
      (recv request-1) (init request-1)))
  (label 78)
  (parent 73)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (syskey answer request answer-0 cookie challenge id secret request-0
      data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request secret) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer-0) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge) (id id) (secret secret)
    (request request-0) (proxy proxy))
  (precedes ((0 0) (1 0)) ((0 0) (3 0)) ((0 2) (1 2)) ((0 2) (3 2))
    ((0 3) (3 3)) ((0 5) (1 3)) ((1 8) (0 6)) ((2 0) (1 7))
    ((3 4) (0 4)) ((3 6) (2 0)))
  (leadsto ((2 0) (1 7)) ((3 6) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation generalization deleted (2 0))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send secret)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request)
      (init request) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((tran request-0 (cat answer-0 cookie)))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0)))
  (label 79)
  (parent 74)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 5) (2 3)) ((1 4) (0 4)) ((2 6) (3 0))
    ((2 8) (0 6)) ((3 0) (2 7)))
  (leadsto ((2 6) (3 0)) ((3 0) (2 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization deleted (3 0))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 80)
  (parent 75)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie
      challenge-0 id-0 secret request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge-0) (id id-0) (secret secret)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 5) (2 3)) ((1 4) (0 4)) ((2 8) (0 6))
    ((3 0) (2 7)) ((4 6) (3 0)))
  (leadsto ((3 0) (2 7)) ((4 6) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization deleted (3 0))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge-0) (send id-0) (recv (enc secret (pubk proxy-0)))
      (recv (enc id-0 (hash secret challenge-0 id-0)))
      (send (enc challenge-0 (hash secret challenge-0 id-0)))
      (recv request-1) (init request-1)))
  (label 81)
  (parent 76)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge id) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 3) (1 3)) ((0 5) (2 0))
    ((1 6) (2 0)) ((1 8) (0 6)) ((2 0) (1 7)))
  (leadsto ((1 6) (2 0)) ((2 0) (1 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig id request)
  (operation generalization weakened ((0 5) (1 6)))
  (traces
    ((send id) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request id id)))
      (recv (enc id (hash request id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash request id id))))))
    ((recv id) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request id id)))
      (send (enc id (hash request id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash request id id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 82)
  (parent 77)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (id syskey request answer request-0 answer-0 cookie challenge id-0
      secret request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge id) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge) (id id-0) (secret secret)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 3) (1 3)) ((0 5) (1 7))
    ((1 8) (0 6)) ((2 0) (1 7)) ((3 6) (2 0)))
  (leadsto ((2 0) (1 7)) ((3 6) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig id request)
  (operation generalization weakened ((0 5) (1 6)))
  (traces
    ((send id) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request id id)))
      (recv (enc id (hash request id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash request id id))))))
    ((recv id) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request id id)))
      (send (enc id (hash request id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash request id id))))))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge) (send id-0) (recv (enc secret (pubk proxy-0)))
      (recv (enc id-0 (hash secret challenge id-0)))
      (send (enc challenge (hash secret challenge id-0)))
      (recv request-1) (init request-1)))
  (label 83)
  (parent 78)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (syskey answer request answer-0 cookie challenge id secret request-0
      data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request secret) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer-0) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge) (id id) (secret secret)
    (request request-0) (proxy proxy))
  (precedes ((0 0) (1 0)) ((0 0) (3 0)) ((0 2) (1 2)) ((0 2) (3 2))
    ((0 3) (3 3)) ((0 5) (1 5)) ((1 8) (0 6)) ((2 0) (1 7))
    ((3 4) (0 4)) ((3 4) (1 3)) ((3 6) (2 0)))
  (leadsto ((2 0) (1 7)) ((3 6) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation generalization weakened ((0 5) (1 3)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send secret)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request)
      (init request) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((tran request-0 (cat answer-0 cookie)))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0)))
  (label 84)
  (parent 79)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 5) (2 5)) ((1 4) (0 4)) ((1 4) (2 3))
    ((2 6) (3 0)) ((2 8) (0 6)) ((3 0) (2 7)))
  (leadsto ((2 6) (3 0)) ((3 0) (2 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((0 5) (2 3)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 85)
  (parent 80)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie
      challenge-0 id-0 secret request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge-0) (id id-0) (secret secret)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 5) (2 5)) ((1 4) (0 4)) ((1 4) (2 3))
    ((2 8) (0 6)) ((3 0) (2 7)) ((4 6) (3 0)))
  (leadsto ((3 0) (2 7)) ((4 6) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((0 5) (2 3)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge-0) (send id-0) (recv (enc secret (pubk proxy-0)))
      (recv (enc id-0 (hash secret challenge-0 id-0)))
      (send (enc challenge-0 (hash secret challenge-0 id-0)))
      (recv request-1) (init request-1)))
  (label 86)
  (parent 81)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge id) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 3) (1 3)) ((0 5) (1 7))
    ((1 6) (2 0)) ((1 8) (0 6)) ((2 0) (1 7)))
  (leadsto ((1 6) (2 0)) ((2 0) (1 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig id request)
  (operation generalization weakened ((0 5) (2 0)))
  (traces
    ((send id) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request id id)))
      (recv (enc id (hash request id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash request id id))))))
    ((recv id) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request id id)))
      (send (enc id (hash request id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash request id id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 87)
  (parent 82)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (id syskey request answer request-0 answer-0 cookie challenge id-0
      secret request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge id) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge) (id id-0) (secret secret)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 3) (1 3)) ((1 8) (0 6))
    ((2 0) (1 7)) ((3 6) (2 0)))
  (leadsto ((2 0) (1 7)) ((3 6) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig id request)
  (operation generalization weakened ((0 5) (1 7)))
  (traces
    ((send id) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request id id)))
      (recv (enc id (hash request id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash request id id))))))
    ((recv id) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request id id)))
      (send (enc id (hash request id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash request id id))))))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge) (send id-0) (recv (enc secret (pubk proxy-0)))
      (recv (enc id-0 (hash secret challenge id-0)))
      (send (enc challenge (hash secret challenge id-0)))
      (recv request-1) (init request-1)))
  (label 88)
  (parent 83)
  (seen 39)
  (unrealized)
  (comment "1 in cohort - 0 not yet seen"))

(defskeleton sbp
  (vars
    (syskey answer request answer-0 cookie challenge id secret request-0
      data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request secret) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer-0) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge) (id id) (secret secret)
    (request request-0) (proxy proxy))
  (precedes ((0 0) (1 0)) ((0 0) (3 0)) ((0 2) (1 2)) ((0 2) (3 2))
    ((0 3) (1 3)) ((0 3) (3 3)) ((0 5) (1 5)) ((1 8) (0 6))
    ((2 0) (1 7)) ((3 1) (1 3)) ((3 4) (0 4)) ((3 6) (2 0)))
  (leadsto ((2 0) (1 7)) ((3 6) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation generalization weakened ((3 4) (1 3)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send secret)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request)
      (init request) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((tran request-0 (cat answer-0 cookie)))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0)))
  (label 89)
  (parent 84)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((0 5) (2 5)) ((1 1) (2 3))
    ((1 4) (0 4)) ((2 6) (3 0)) ((2 8) (0 6)) ((3 0) (2 7)))
  (leadsto ((2 6) (3 0)) ((3 0) (2 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((1 4) (2 3)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 90)
  (parent 85)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie
      challenge-0 id-0 secret request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge-0) (id id-0) (secret secret)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((0 5) (2 5)) ((1 1) (2 3))
    ((1 4) (0 4)) ((2 8) (0 6)) ((3 0) (2 7)) ((4 6) (3 0)))
  (leadsto ((3 0) (2 7)) ((4 6) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((1 4) (2 3)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge-0) (send id-0) (recv (enc secret (pubk proxy-0)))
      (recv (enc id-0 (hash secret challenge-0 id-0)))
      (send (enc challenge-0 (hash secret challenge-0 id-0)))
      (recv request-1) (init request-1)))
  (label 91)
  (parent 86)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars (id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge id) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge id) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((0 3) (1 3)) ((1 6) (2 0))
    ((1 8) (0 6)) ((2 0) (1 7)))
  (leadsto ((1 6) (2 0)) ((2 0) (1 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig id request)
  (operation generalization weakened ((0 5) (1 7)))
  (traces
    ((send id) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request id id)))
      (recv (enc id (hash request id id))) (send request)
      (recv
        (cat answer (enc cookie (hash syskey (hash request id id))))))
    ((recv id) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request id id)))
      (send (enc id (hash request id id))) (recv request-0)
      (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0 (enc cookie (hash syskey (hash request id id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 92)
  (parent 87)
  (seen 38)
  (unrealized)
  (comment "1 in cohort - 0 not yet seen"))

(defskeleton sbp
  (vars
    (syskey answer request answer-0 cookie challenge id secret request-0
      data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request secret) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer-0) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge) (id id) (secret secret)
    (request request-0) (proxy proxy))
  (precedes ((0 0) (1 0)) ((0 0) (3 0)) ((0 2) (1 2)) ((0 2) (3 2))
    ((0 3) (1 3)) ((0 3) (3 3)) ((0 5) (1 5)) ((1 8) (0 6))
    ((2 0) (1 7)) ((3 4) (0 4)) ((3 6) (2 0)))
  (leadsto ((2 0) (1 7)) ((3 6) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation generalization weakened ((3 1) (1 3)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send secret)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request)
      (init request) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((tran request-0 (cat answer-0 cookie)))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0)))
  (label 93)
  (parent 89)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((0 5) (2 5)) ((1 4) (0 4))
    ((2 6) (3 0)) ((2 8) (0 6)) ((3 0) (2 7)))
  (leadsto ((2 6) (3 0)) ((3 0) (2 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((1 1) (2 3)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 94)
  (parent 90)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie
      challenge-0 id-0 secret request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge-0) (id id-0) (secret secret)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((0 5) (2 5)) ((1 4) (0 4))
    ((2 8) (0 6)) ((3 0) (2 7)) ((4 6) (3 0)))
  (leadsto ((3 0) (2 7)) ((4 6) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((1 1) (2 3)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge-0) (send id-0) (recv (enc secret (pubk proxy-0)))
      (recv (enc id-0 (hash secret challenge-0 id-0)))
      (send (enc challenge-0 (hash secret challenge-0 id-0)))
      (recv request-1) (init request-1)))
  (label 95)
  (parent 91)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (syskey answer request answer-0 cookie challenge id secret request-0
      data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request secret) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer-0) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge) (id id) (secret secret)
    (request request-0) (proxy proxy))
  (precedes ((0 0) (1 0)) ((0 0) (3 0)) ((0 2) (1 2)) ((0 2) (3 2))
    ((0 3) (1 3)) ((0 3) (3 3)) ((0 5) (1 6)) ((1 8) (0 6))
    ((2 0) (1 7)) ((3 4) (0 4)) ((3 4) (1 5)) ((3 6) (2 0)))
  (leadsto ((2 0) (1 7)) ((3 6) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation generalization weakened ((0 5) (1 5)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send secret)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request)
      (init request) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((tran request-0 (cat answer-0 cookie)))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0)))
  (label 96)
  (parent 93)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((0 5) (2 6)) ((1 4) (0 4))
    ((1 4) (2 5)) ((2 6) (3 0)) ((2 8) (0 6)) ((3 0) (2 7)))
  (leadsto ((2 6) (3 0)) ((3 0) (2 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((0 5) (2 5)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 97)
  (parent 94)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie
      challenge-0 id-0 secret request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge-0) (id id-0) (secret secret)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((0 5) (2 6)) ((1 4) (0 4))
    ((1 4) (2 5)) ((2 8) (0 6)) ((3 0) (2 7)) ((4 6) (3 0)))
  (leadsto ((3 0) (2 7)) ((4 6) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((0 5) (2 5)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge-0) (send id-0) (recv (enc secret (pubk proxy-0)))
      (recv (enc id-0 (hash secret challenge-0 id-0)))
      (send (enc challenge-0 (hash secret challenge-0 id-0)))
      (recv request-1) (init request-1)))
  (label 98)
  (parent 95)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (syskey answer request answer-0 cookie challenge id secret request-0
      data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request secret) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer-0) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge) (id id) (secret secret)
    (request request-0) (proxy proxy))
  (precedes ((0 0) (1 0)) ((0 0) (3 0)) ((0 2) (1 2)) ((0 2) (3 2))
    ((0 3) (1 3)) ((0 3) (3 3)) ((0 5) (1 6)) ((1 8) (0 6))
    ((2 0) (1 7)) ((3 1) (1 5)) ((3 4) (0 4)) ((3 6) (2 0)))
  (leadsto ((2 0) (1 7)) ((3 6) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation generalization weakened ((3 4) (1 5)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send secret)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request)
      (init request) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((tran request-0 (cat answer-0 cookie)))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0)))
  (label 99)
  (parent 96)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((0 5) (2 6)) ((1 1) (2 5))
    ((1 4) (0 4)) ((2 6) (3 0)) ((2 8) (0 6)) ((3 0) (2 7)))
  (leadsto ((2 6) (3 0)) ((3 0) (2 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((1 4) (2 5)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 100)
  (parent 97)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie
      challenge-0 id-0 secret request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge-0) (id id-0) (secret secret)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((0 5) (2 6)) ((1 1) (2 5))
    ((1 4) (0 4)) ((2 8) (0 6)) ((3 0) (2 7)) ((4 6) (3 0)))
  (leadsto ((3 0) (2 7)) ((4 6) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((1 4) (2 5)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge-0) (send id-0) (recv (enc secret (pubk proxy-0)))
      (recv (enc id-0 (hash secret challenge-0 id-0)))
      (send (enc challenge-0 (hash secret challenge-0 id-0)))
      (recv request-1) (init request-1)))
  (label 101)
  (parent 98)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (syskey answer request answer-0 cookie challenge id secret request-0
      data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request secret) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer-0) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge) (id id) (secret secret)
    (request request-0) (proxy proxy))
  (precedes ((0 0) (1 0)) ((0 0) (3 0)) ((0 2) (1 2)) ((0 2) (3 2))
    ((0 3) (1 3)) ((0 3) (3 3)) ((0 5) (1 6)) ((1 8) (0 6))
    ((2 0) (1 7)) ((3 4) (0 4)) ((3 6) (2 0)))
  (leadsto ((2 0) (1 7)) ((3 6) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation generalization weakened ((3 1) (1 5)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send secret)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request)
      (init request) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((tran request-0 (cat answer-0 cookie)))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0)))
  (label 102)
  (parent 99)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((0 5) (2 6)) ((1 4) (0 4))
    ((2 6) (3 0)) ((2 8) (0 6)) ((3 0) (2 7)))
  (leadsto ((2 6) (3 0)) ((3 0) (2 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((1 1) (2 5)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 103)
  (parent 100)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie
      challenge-0 id-0 secret request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge-0) (id id-0) (secret secret)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((0 5) (2 6)) ((1 4) (0 4))
    ((2 8) (0 6)) ((3 0) (2 7)) ((4 6) (3 0)))
  (leadsto ((3 0) (2 7)) ((4 6) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((1 1) (2 5)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge-0) (send id-0) (recv (enc secret (pubk proxy-0)))
      (recv (enc id-0 (hash secret challenge-0 id-0)))
      (send (enc challenge-0 (hash secret challenge-0 id-0)))
      (recv request-1) (init request-1)))
  (label 104)
  (parent 101)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (syskey answer request answer-0 cookie challenge id secret request-0
      data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request secret) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer-0) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge) (id id) (secret secret)
    (request request-0) (proxy proxy))
  (precedes ((0 0) (1 0)) ((0 0) (3 0)) ((0 2) (1 2)) ((0 2) (3 2))
    ((0 3) (1 3)) ((0 3) (3 3)) ((0 5) (1 7)) ((1 8) (0 6))
    ((2 0) (1 7)) ((3 4) (0 4)) ((3 4) (1 6)) ((3 6) (2 0)))
  (leadsto ((2 0) (1 7)) ((3 6) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation generalization weakened ((0 5) (1 6)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send secret)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request)
      (init request) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((tran request-0 (cat answer-0 cookie)))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0)))
  (label 105)
  (parent 102)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((0 5) (3 0)) ((1 4) (0 4))
    ((1 4) (2 6)) ((2 6) (3 0)) ((2 8) (0 6)) ((3 0) (2 7)))
  (leadsto ((2 6) (3 0)) ((3 0) (2 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((0 5) (2 6)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 106)
  (parent 103)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie
      challenge-0 id-0 secret request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge-0) (id id-0) (secret secret)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((0 5) (2 7)) ((1 4) (0 4))
    ((1 4) (2 6)) ((2 8) (0 6)) ((3 0) (2 7)) ((4 6) (3 0)))
  (leadsto ((3 0) (2 7)) ((4 6) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((0 5) (2 6)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge-0) (send id-0) (recv (enc secret (pubk proxy-0)))
      (recv (enc id-0 (hash secret challenge-0 id-0)))
      (send (enc challenge-0 (hash secret challenge-0 id-0)))
      (recv request-1) (init request-1)))
  (label 107)
  (parent 104)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (syskey answer request answer-0 cookie challenge id secret request-0
      data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request secret) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer-0) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge) (id id) (secret secret)
    (request request-0) (proxy proxy))
  (precedes ((0 0) (1 0)) ((0 0) (3 0)) ((0 2) (1 2)) ((0 2) (3 2))
    ((0 3) (1 3)) ((0 3) (3 3)) ((0 5) (1 7)) ((1 8) (0 6))
    ((2 0) (1 7)) ((3 1) (1 6)) ((3 4) (0 4)) ((3 6) (2 0)))
  (leadsto ((2 0) (1 7)) ((3 6) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation generalization weakened ((3 4) (1 6)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send secret)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request)
      (init request) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((tran request-0 (cat answer-0 cookie)))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0)))
  (label 108)
  (parent 105)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((0 5) (3 0)) ((1 1) (2 6))
    ((1 4) (0 4)) ((2 6) (3 0)) ((2 8) (0 6)) ((3 0) (2 7)))
  (leadsto ((2 6) (3 0)) ((3 0) (2 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((1 4) (2 6)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 109)
  (parent 106)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie
      challenge-0 id-0 secret request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge-0) (id id-0) (secret secret)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((0 5) (2 7)) ((1 1) (2 6))
    ((1 4) (0 4)) ((2 8) (0 6)) ((3 0) (2 7)) ((4 6) (3 0)))
  (leadsto ((3 0) (2 7)) ((4 6) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((1 4) (2 6)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge-0) (send id-0) (recv (enc secret (pubk proxy-0)))
      (recv (enc id-0 (hash secret challenge-0 id-0)))
      (send (enc challenge-0 (hash secret challenge-0 id-0)))
      (recv request-1) (init request-1)))
  (label 110)
  (parent 107)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (syskey answer request answer-0 cookie challenge id secret request-0
      data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request secret) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer-0) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge) (id id) (secret secret)
    (request request-0) (proxy proxy))
  (precedes ((0 0) (1 0)) ((0 0) (3 0)) ((0 2) (1 2)) ((0 2) (3 2))
    ((0 3) (1 3)) ((0 3) (3 3)) ((0 5) (1 7)) ((1 8) (0 6))
    ((2 0) (1 7)) ((3 4) (0 4)) ((3 6) (2 0)))
  (leadsto ((2 0) (1 7)) ((3 6) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation generalization weakened ((3 1) (1 6)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send secret)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request)
      (init request) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((tran request-0 (cat answer-0 cookie)))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0)))
  (label 111)
  (parent 108)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((0 5) (3 0)) ((1 4) (0 4))
    ((2 6) (3 0)) ((2 8) (0 6)) ((3 0) (2 7)))
  (leadsto ((2 6) (3 0)) ((3 0) (2 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((1 1) (2 6)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 112)
  (parent 109)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie
      challenge-0 id-0 secret request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge-0) (id id-0) (secret secret)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((0 5) (2 7)) ((1 4) (0 4))
    ((2 8) (0 6)) ((3 0) (2 7)) ((4 6) (3 0)))
  (leadsto ((3 0) (2 7)) ((4 6) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((1 1) (2 6)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge-0) (send id-0) (recv (enc secret (pubk proxy-0)))
      (recv (enc id-0 (hash secret challenge-0 id-0)))
      (send (enc challenge-0 (hash secret challenge-0 id-0)))
      (recv request-1) (init request-1)))
  (label 113)
  (parent 110)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (syskey answer request answer-0 cookie challenge id secret request-0
      data) (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret secret)
    (request secret) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret secret)
    (request request) (answer answer-0) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge) (id id) (secret secret)
    (request request-0) (proxy proxy))
  (precedes ((0 0) (1 0)) ((0 0) (3 0)) ((0 2) (1 2)) ((0 2) (3 2))
    ((0 3) (1 3)) ((0 3) (3 3)) ((1 8) (0 6)) ((2 0) (1 7))
    ((3 4) (0 4)) ((3 6) (2 0)))
  (leadsto ((2 0) (1 7)) ((3 6) (2 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge secret)
  (operation generalization weakened ((0 5) (1 7)))
  (traces
    ((send challenge) (recv id) (send (enc secret (pubk proxy)))
      (send (enc id (hash secret challenge id)))
      (recv (enc challenge (hash secret challenge id))) (send secret)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request)
      (init request) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash secret challenge id))))))
    ((tran request-0 (cat answer-0 cookie)))
    ((recv challenge) (send id) (recv (enc secret (pubk proxy)))
      (recv (enc id (hash secret challenge id)))
      (send (enc challenge (hash secret challenge id))) (recv request-0)
      (init request-0)))
  (label 114)
  (parent 111)
  (seen 56)
  (unrealized)
  (comment "1 in cohort - 0 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((0 5) (2 7)) ((1 4) (0 4))
    ((1 4) (3 0)) ((2 6) (3 0)) ((2 8) (0 6)) ((3 0) (2 7)))
  (leadsto ((2 6) (3 0)) ((3 0) (2 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((0 5) (3 0)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 115)
  (parent 112)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie
      challenge-0 id-0 secret request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge-0) (id id-0) (secret secret)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((1 4) (0 4)) ((1 4) (2 7))
    ((2 8) (0 6)) ((3 0) (2 7)) ((4 6) (3 0)))
  (leadsto ((3 0) (2 7)) ((4 6) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((0 5) (2 7)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge-0) (send id-0) (recv (enc secret (pubk proxy-0)))
      (recv (enc id-0 (hash secret challenge-0 id-0)))
      (send (enc challenge-0 (hash secret challenge-0 id-0)))
      (recv request-1) (init request-1)))
  (label 116)
  (parent 113)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((0 5) (2 7)) ((1 1) (3 0))
    ((1 4) (0 4)) ((2 6) (3 0)) ((2 8) (0 6)) ((3 0) (2 7)))
  (leadsto ((2 6) (3 0)) ((3 0) (2 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((1 4) (3 0)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 117)
  (parent 115)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie
      challenge-0 id-0 secret request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge-0) (id id-0) (secret secret)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((1 1) (2 7)) ((1 4) (0 4))
    ((2 8) (0 6)) ((3 0) (2 7)) ((4 6) (3 0)))
  (leadsto ((3 0) (2 7)) ((4 6) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((1 4) (2 7)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge-0) (send id-0) (recv (enc secret (pubk proxy-0)))
      (recv (enc id-0 (hash secret challenge-0 id-0)))
      (send (enc challenge-0 (hash secret challenge-0 id-0)))
      (recv request-1) (init request-1)))
  (label 118)
  (parent 116)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((0 5) (2 7)) ((1 4) (0 4))
    ((2 6) (3 0)) ((2 8) (0 6)) ((3 0) (2 7)))
  (leadsto ((2 6) (3 0)) ((3 0) (2 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((1 1) (3 0)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 119)
  (parent 117)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie
      challenge-0 id-0 secret request-1 data) (proxy proxy-0 name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-1) (answer answer-0)
    (cookie cookie))
  (defstrand proxy 7 (challenge challenge-0) (id id-0) (secret secret)
    (request request-1) (proxy proxy-0))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((1 4) (0 4)) ((2 8) (0 6))
    ((3 0) (2 7)) ((4 6) (3 0)))
  (leadsto ((3 0) (2 7)) ((4 6) (3 0)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((1 1) (2 7)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-1 (cat answer-0 cookie)))
    ((recv challenge-0) (send id-0) (recv (enc secret (pubk proxy-0)))
      (recv (enc id-0 (hash secret challenge-0 id-0)))
      (send (enc challenge-0 (hash secret challenge-0 id-0)))
      (recv request-1) (init request-1)))
  (label 120)
  (parent 118)
  (seen 58)
  (unrealized)
  (comment "1 in cohort - 0 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((1 4) (0 4)) ((1 4) (2 7))
    ((2 6) (3 0)) ((2 8) (0 6)) ((3 0) (2 7)))
  (leadsto ((2 6) (3 0)) ((3 0) (2 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((0 5) (2 7)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 121)
  (parent 119)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((1 1) (2 7)) ((1 4) (0 4))
    ((2 6) (3 0)) ((2 8) (0 6)) ((3 0) (2 7)))
  (leadsto ((2 6) (3 0)) ((3 0) (2 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((1 4) (2 7)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 122)
  (parent 121)
  (unrealized)
  (comment "1 in cohort - 1 not yet seen"))

(defskeleton sbp
  (vars
    (challenge id syskey request answer request-0 answer-0 cookie data)
    (proxy name))
  (defstrand client 7 (challenge challenge) (id id) (secret request)
    (request request) (answer answer) (cookie cookie) (syskey syskey)
    (proxy proxy))
  (defstrand proxy 5 (challenge challenge) (id id) (secret request)
    (proxy proxy))
  (defstrand proxy 9 (challenge challenge) (id id) (secret request)
    (request request-0) (answer answer-0) (cookie cookie)
    (syskey syskey) (proxy proxy))
  (defstrand server 1 (request request-0) (answer answer-0)
    (cookie cookie))
  (precedes ((0 0) (1 0)) ((0 0) (2 0)) ((0 2) (1 2)) ((0 2) (2 2))
    ((0 3) (1 3)) ((0 3) (2 3)) ((1 4) (0 4)) ((2 6) (3 0))
    ((2 8) (0 6)) ((3 0) (2 7)))
  (leadsto ((2 6) (3 0)) ((3 0) (2 7)))
  (non-orig syskey (privk proxy))
  (pen-non-orig (pubk proxy))
  (uniq-orig challenge request)
  (operation generalization weakened ((1 1) (2 7)))
  (traces
    ((send challenge) (recv id) (send (enc request (pubk proxy)))
      (send (enc id (hash request challenge id)))
      (recv (enc challenge (hash request challenge id))) (send request)
      (recv
        (cat answer
          (enc cookie (hash syskey (hash request challenge id))))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id))))
    ((recv challenge) (send id) (recv (enc request (pubk proxy)))
      (recv (enc id (hash request challenge id)))
      (send (enc challenge (hash request challenge id)))
      (recv request-0) (init request-0) (obsv (cat answer-0 cookie))
      (send
        (cat answer-0
          (enc cookie (hash syskey (hash request challenge id))))))
    ((tran request-0 (cat answer-0 cookie))))
  (label 123)
  (parent 122)
  (seen 57)
  (unrealized)
  (comment "1 in cohort - 0 not yet seen"))

(comment "Nothing left to do")
