(comment "CPSA 3.6.6")
(comment "Extracted shapes")

(herald "Session Binding Protocol (SBP)")

(comment "CPSA 3.6.6")

(comment "All input read from sbp_tls.lsp")

(defprotocol sbp basic
  (defrole client
    (vars (cc id s cred request answer data) (pk akey)
      (enc_cookie mesg))
    (trace (send cc) (recv (cat id pk)) (send (enc s pk))
      (send (enc id (hash s cc id))) (recv (enc cc (hash s cc id)))
      (send (enc "login-request" cred (hash s cc id)))
      (recv (enc "login-success" enc_cookie (hash s cc id)))
      (send (enc "request" request enc_cookie (hash s cc id)))
      (recv (enc "answer" answer (hash s cc id))))
    (uniq-gen cc s))
  (defrole proxy
    (vars (cc id s cred cookie request answer sskey data) (pk akey))
    (trace (recv cc) (send (cat id pk)) (recv (enc s pk))
      (send (enc id (hash s cc id))) (recv (enc cc (hash s cc id)))
      (recv (enc "login-request" cred (hash s cc id)))
      (send
        (enc "login-success" (enc cookie (hash sskey (hash s cc id)))
          (hash s cc id)))
      (recv
        (enc "request" request (enc cookie (hash sskey (hash s cc id)))
          (hash s cc id)))
      (send
        (enc "answer" answer (enc cookie (hash sskey (hash s cc id)))
          (hash s cc id))))
    (non-orig sskey (invk pk))
    (uniq-orig answer)
    (uniq-gen id cookie)))

(defskeleton sbp
  (vars (enc_cookie mesg) (answer cc id s cred request data) (pk akey))
  (defstrand client 9 (enc_cookie enc_cookie) (cc cc) (id id) (s s)
    (cred cred) (request request) (answer answer) (pk pk))
  (deflistener answer)
  (uniq-gen cc s)
  (traces
    ((send cc) (recv (cat id pk)) (send (enc s pk))
      (send (enc id (hash s cc id))) (recv (enc cc (hash s cc id)))
      (send (enc "login-request" cred (hash s cc id)))
      (recv (enc "login-success" enc_cookie (hash s cc id)))
      (send (enc "request" request enc_cookie (hash s cc id)))
      (recv (enc "answer" answer (hash s cc id))))
    ((recv answer) (send answer)))
  (label 0)
  (unrealized)
  (shape)
  (maps
    ((0 1)
      ((answer answer) (cc cc) (id id) (s s) (cred cred)
        (request request) (pk pk) (enc_cookie enc_cookie))))
  (origs))

(comment "Nothing left to do")
